// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vote/entities/vote_category.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { User } from "../../user/entities/user";
import { VoterType, voterTypeFromJSON, voterTypeToJSON } from "./voter_type";
import { VotingMethod, votingMethodFromJSON, votingMethodToJSON } from "./voting_method";

export const protobufPackage = "vote.entities";

/**
 * VoteCategory represents a voting category within a hackathon, defining
 * the criteria and rules for one dimension of evaluation.
 */
export interface VoteCategory {
  id: string;
  hackathonId: string;
  name: string;
  description: string;
  votingMethod: VotingMethod;
  voterType: VoterType;
  maxPoints?: number | undefined;
  juryMembers: User[];
  createdAt: number;
  modifiedAt: number;
}

function createBaseVoteCategory(): VoteCategory {
  return {
    id: "",
    hackathonId: "",
    name: "",
    description: "",
    votingMethod: 0,
    voterType: 0,
    maxPoints: undefined,
    juryMembers: [],
    createdAt: 0,
    modifiedAt: 0,
  };
}

export const VoteCategory: MessageFns<VoteCategory> = {
  encode(message: VoteCategory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.hackathonId !== "") {
      writer.uint32(18).string(message.hackathonId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.votingMethod !== 0) {
      writer.uint32(40).int32(message.votingMethod);
    }
    if (message.voterType !== 0) {
      writer.uint32(48).int32(message.voterType);
    }
    if (message.maxPoints !== undefined) {
      writer.uint32(56).int32(message.maxPoints);
    }
    for (const v of message.juryMembers) {
      User.encode(v!, writer.uint32(66).fork()).join();
    }
    if (message.createdAt !== 0) {
      writer.uint32(72).int64(message.createdAt);
    }
    if (message.modifiedAt !== 0) {
      writer.uint32(80).int64(message.modifiedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteCategory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteCategory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.hackathonId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.votingMethod = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.voterType = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxPoints = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.juryMembers.push(User.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.modifiedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoteCategory {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      hackathonId: isSet(object.hackathonId)
        ? globalThis.String(object.hackathonId)
        : isSet(object.hackathon_id)
        ? globalThis.String(object.hackathon_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      votingMethod: isSet(object.votingMethod)
        ? votingMethodFromJSON(object.votingMethod)
        : isSet(object.voting_method)
        ? votingMethodFromJSON(object.voting_method)
        : 0,
      voterType: isSet(object.voterType)
        ? voterTypeFromJSON(object.voterType)
        : isSet(object.voter_type)
        ? voterTypeFromJSON(object.voter_type)
        : 0,
      maxPoints: isSet(object.maxPoints)
        ? globalThis.Number(object.maxPoints)
        : isSet(object.max_points)
        ? globalThis.Number(object.max_points)
        : undefined,
      juryMembers: globalThis.Array.isArray(object?.juryMembers)
        ? object.juryMembers.map((e: any) => User.fromJSON(e))
        : globalThis.Array.isArray(object?.jury_members)
        ? object.jury_members.map((e: any) => User.fromJSON(e))
        : [],
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.Number(object.created_at)
        : 0,
      modifiedAt: isSet(object.modifiedAt)
        ? globalThis.Number(object.modifiedAt)
        : isSet(object.modified_at)
        ? globalThis.Number(object.modified_at)
        : 0,
    };
  },

  toJSON(message: VoteCategory): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.hackathonId !== "") {
      obj.hackathonId = message.hackathonId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.votingMethod !== 0) {
      obj.votingMethod = votingMethodToJSON(message.votingMethod);
    }
    if (message.voterType !== 0) {
      obj.voterType = voterTypeToJSON(message.voterType);
    }
    if (message.maxPoints !== undefined) {
      obj.maxPoints = Math.round(message.maxPoints);
    }
    if (message.juryMembers?.length) {
      obj.juryMembers = message.juryMembers.map((e) => User.toJSON(e));
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.modifiedAt !== 0) {
      obj.modifiedAt = Math.round(message.modifiedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<VoteCategory>): VoteCategory {
    return VoteCategory.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoteCategory>): VoteCategory {
    const message = createBaseVoteCategory();
    message.id = object.id ?? "";
    message.hackathonId = object.hackathonId ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.votingMethod = object.votingMethod ?? 0;
    message.voterType = object.voterType ?? 0;
    message.maxPoints = object.maxPoints ?? undefined;
    message.juryMembers = object.juryMembers?.map((e) => User.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? 0;
    message.modifiedAt = object.modifiedAt ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
