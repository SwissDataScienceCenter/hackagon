// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vote/messages/vote_svc/list_categories_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { VoteCategory } from "../../entities/vote_category";

export const protobufPackage = "vote.messages.vote_svc";

export interface ListVoteCategoriesResponse {
  voteCategories: VoteCategory[];
}

function createBaseListVoteCategoriesResponse(): ListVoteCategoriesResponse {
  return { voteCategories: [] };
}

export const ListVoteCategoriesResponse: MessageFns<ListVoteCategoriesResponse> = {
  encode(message: ListVoteCategoriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.voteCategories) {
      VoteCategory.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListVoteCategoriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListVoteCategoriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.voteCategories.push(VoteCategory.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListVoteCategoriesResponse {
    return {
      voteCategories: globalThis.Array.isArray(object?.voteCategories)
        ? object.voteCategories.map((e: any) => VoteCategory.fromJSON(e))
        : globalThis.Array.isArray(object?.vote_categories)
        ? object.vote_categories.map((e: any) => VoteCategory.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListVoteCategoriesResponse): unknown {
    const obj: any = {};
    if (message.voteCategories?.length) {
      obj.voteCategories = message.voteCategories.map((e) => VoteCategory.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListVoteCategoriesResponse>): ListVoteCategoriesResponse {
    return ListVoteCategoriesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListVoteCategoriesResponse>): ListVoteCategoriesResponse {
    const message = createBaseListVoteCategoriesResponse();
    message.voteCategories = object.voteCategories?.map((e) => VoteCategory.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
