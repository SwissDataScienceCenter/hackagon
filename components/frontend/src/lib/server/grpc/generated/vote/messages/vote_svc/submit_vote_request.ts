// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vote/messages/vote_svc/submit_vote_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "vote.messages.vote_svc";

export interface SubmitVoteRequest {
  categoryId: string;
  singleChoice?: SingleChoiceVote | undefined;
  ranked?: RankedVote | undefined;
  points?: PointsVote | undefined;
}

export interface SingleChoiceVote {
  submissionId: string;
}

export interface RankedVote {
  submissions: RankedSubmission[];
}

export interface RankedSubmission {
  submissionId: string;
  rank: number;
}

export interface PointsVote {
  submissions: PointsSubmission[];
}

export interface PointsSubmission {
  submissionId: string;
  points: number;
}

function createBaseSubmitVoteRequest(): SubmitVoteRequest {
  return { categoryId: "", singleChoice: undefined, ranked: undefined, points: undefined };
}

export const SubmitVoteRequest: MessageFns<SubmitVoteRequest> = {
  encode(message: SubmitVoteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.categoryId !== "") {
      writer.uint32(10).string(message.categoryId);
    }
    if (message.singleChoice !== undefined) {
      SingleChoiceVote.encode(message.singleChoice, writer.uint32(18).fork()).join();
    }
    if (message.ranked !== undefined) {
      RankedVote.encode(message.ranked, writer.uint32(26).fork()).join();
    }
    if (message.points !== undefined) {
      PointsVote.encode(message.points, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubmitVoteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubmitVoteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.categoryId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.singleChoice = SingleChoiceVote.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ranked = RankedVote.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.points = PointsVote.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubmitVoteRequest {
    return {
      categoryId: isSet(object.categoryId)
        ? globalThis.String(object.categoryId)
        : isSet(object.category_id)
        ? globalThis.String(object.category_id)
        : "",
      singleChoice: isSet(object.singleChoice)
        ? SingleChoiceVote.fromJSON(object.singleChoice)
        : isSet(object.single_choice)
        ? SingleChoiceVote.fromJSON(object.single_choice)
        : undefined,
      ranked: isSet(object.ranked) ? RankedVote.fromJSON(object.ranked) : undefined,
      points: isSet(object.points) ? PointsVote.fromJSON(object.points) : undefined,
    };
  },

  toJSON(message: SubmitVoteRequest): unknown {
    const obj: any = {};
    if (message.categoryId !== "") {
      obj.categoryId = message.categoryId;
    }
    if (message.singleChoice !== undefined) {
      obj.singleChoice = SingleChoiceVote.toJSON(message.singleChoice);
    }
    if (message.ranked !== undefined) {
      obj.ranked = RankedVote.toJSON(message.ranked);
    }
    if (message.points !== undefined) {
      obj.points = PointsVote.toJSON(message.points);
    }
    return obj;
  },

  create(base?: DeepPartial<SubmitVoteRequest>): SubmitVoteRequest {
    return SubmitVoteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SubmitVoteRequest>): SubmitVoteRequest {
    const message = createBaseSubmitVoteRequest();
    message.categoryId = object.categoryId ?? "";
    message.singleChoice = (object.singleChoice !== undefined && object.singleChoice !== null)
      ? SingleChoiceVote.fromPartial(object.singleChoice)
      : undefined;
    message.ranked = (object.ranked !== undefined && object.ranked !== null)
      ? RankedVote.fromPartial(object.ranked)
      : undefined;
    message.points = (object.points !== undefined && object.points !== null)
      ? PointsVote.fromPartial(object.points)
      : undefined;
    return message;
  },
};

function createBaseSingleChoiceVote(): SingleChoiceVote {
  return { submissionId: "" };
}

export const SingleChoiceVote: MessageFns<SingleChoiceVote> = {
  encode(message: SingleChoiceVote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.submissionId !== "") {
      writer.uint32(10).string(message.submissionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleChoiceVote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleChoiceVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.submissionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SingleChoiceVote {
    return {
      submissionId: isSet(object.submissionId)
        ? globalThis.String(object.submissionId)
        : isSet(object.submission_id)
        ? globalThis.String(object.submission_id)
        : "",
    };
  },

  toJSON(message: SingleChoiceVote): unknown {
    const obj: any = {};
    if (message.submissionId !== "") {
      obj.submissionId = message.submissionId;
    }
    return obj;
  },

  create(base?: DeepPartial<SingleChoiceVote>): SingleChoiceVote {
    return SingleChoiceVote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SingleChoiceVote>): SingleChoiceVote {
    const message = createBaseSingleChoiceVote();
    message.submissionId = object.submissionId ?? "";
    return message;
  },
};

function createBaseRankedVote(): RankedVote {
  return { submissions: [] };
}

export const RankedVote: MessageFns<RankedVote> = {
  encode(message: RankedVote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.submissions) {
      RankedSubmission.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RankedVote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRankedVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.submissions.push(RankedSubmission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RankedVote {
    return {
      submissions: globalThis.Array.isArray(object?.submissions)
        ? object.submissions.map((e: any) => RankedSubmission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RankedVote): unknown {
    const obj: any = {};
    if (message.submissions?.length) {
      obj.submissions = message.submissions.map((e) => RankedSubmission.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<RankedVote>): RankedVote {
    return RankedVote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RankedVote>): RankedVote {
    const message = createBaseRankedVote();
    message.submissions = object.submissions?.map((e) => RankedSubmission.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRankedSubmission(): RankedSubmission {
  return { submissionId: "", rank: 0 };
}

export const RankedSubmission: MessageFns<RankedSubmission> = {
  encode(message: RankedSubmission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.submissionId !== "") {
      writer.uint32(10).string(message.submissionId);
    }
    if (message.rank !== 0) {
      writer.uint32(16).int32(message.rank);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RankedSubmission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRankedSubmission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.submissionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.rank = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RankedSubmission {
    return {
      submissionId: isSet(object.submissionId)
        ? globalThis.String(object.submissionId)
        : isSet(object.submission_id)
        ? globalThis.String(object.submission_id)
        : "",
      rank: isSet(object.rank) ? globalThis.Number(object.rank) : 0,
    };
  },

  toJSON(message: RankedSubmission): unknown {
    const obj: any = {};
    if (message.submissionId !== "") {
      obj.submissionId = message.submissionId;
    }
    if (message.rank !== 0) {
      obj.rank = Math.round(message.rank);
    }
    return obj;
  },

  create(base?: DeepPartial<RankedSubmission>): RankedSubmission {
    return RankedSubmission.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RankedSubmission>): RankedSubmission {
    const message = createBaseRankedSubmission();
    message.submissionId = object.submissionId ?? "";
    message.rank = object.rank ?? 0;
    return message;
  },
};

function createBasePointsVote(): PointsVote {
  return { submissions: [] };
}

export const PointsVote: MessageFns<PointsVote> = {
  encode(message: PointsVote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.submissions) {
      PointsSubmission.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PointsVote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePointsVote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.submissions.push(PointsSubmission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PointsVote {
    return {
      submissions: globalThis.Array.isArray(object?.submissions)
        ? object.submissions.map((e: any) => PointsSubmission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PointsVote): unknown {
    const obj: any = {};
    if (message.submissions?.length) {
      obj.submissions = message.submissions.map((e) => PointsSubmission.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PointsVote>): PointsVote {
    return PointsVote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PointsVote>): PointsVote {
    const message = createBasePointsVote();
    message.submissions = object.submissions?.map((e) => PointsSubmission.fromPartial(e)) || [];
    return message;
  },
};

function createBasePointsSubmission(): PointsSubmission {
  return { submissionId: "", points: 0 };
}

export const PointsSubmission: MessageFns<PointsSubmission> = {
  encode(message: PointsSubmission, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.submissionId !== "") {
      writer.uint32(10).string(message.submissionId);
    }
    if (message.points !== 0) {
      writer.uint32(16).int32(message.points);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PointsSubmission {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePointsSubmission();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.submissionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.points = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PointsSubmission {
    return {
      submissionId: isSet(object.submissionId)
        ? globalThis.String(object.submissionId)
        : isSet(object.submission_id)
        ? globalThis.String(object.submission_id)
        : "",
      points: isSet(object.points) ? globalThis.Number(object.points) : 0,
    };
  },

  toJSON(message: PointsSubmission): unknown {
    const obj: any = {};
    if (message.submissionId !== "") {
      obj.submissionId = message.submissionId;
    }
    if (message.points !== 0) {
      obj.points = Math.round(message.points);
    }
    return obj;
  },

  create(base?: DeepPartial<PointsSubmission>): PointsSubmission {
    return PointsSubmission.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PointsSubmission>): PointsSubmission {
    const message = createBasePointsSubmission();
    message.submissionId = object.submissionId ?? "";
    message.points = object.points ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
