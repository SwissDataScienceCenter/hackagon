// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vote/messages/vote_svc/suggest_results_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { VoteResult } from "../../entities/vote_result";

export const protobufPackage = "vote.messages.vote_svc";

export interface SuggestResultsResponse {
  results: VoteResult[];
}

function createBaseSuggestResultsResponse(): SuggestResultsResponse {
  return { results: [] };
}

export const SuggestResultsResponse: MessageFns<SuggestResultsResponse> = {
  encode(message: SuggestResultsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      VoteResult.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SuggestResultsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSuggestResultsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(VoteResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SuggestResultsResponse {
    return {
      results: globalThis.Array.isArray(object?.results) ? object.results.map((e: any) => VoteResult.fromJSON(e)) : [],
    };
  },

  toJSON(message: SuggestResultsResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => VoteResult.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SuggestResultsResponse>): SuggestResultsResponse {
    return SuggestResultsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SuggestResultsResponse>): SuggestResultsResponse {
    const message = createBaseSuggestResultsResponse();
    message.results = object.results?.map((e) => VoteResult.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
