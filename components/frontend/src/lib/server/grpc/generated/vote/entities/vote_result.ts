// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: vote/entities/vote_result.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "vote.entities";

/** VoteResult is a placement entry within a vote category. */
export interface VoteResult {
  id: string;
  categoryId: string;
  submissionId: string;
  position: number;
  title?: string | undefined;
  createdAt: number;
  modifiedAt: number;
}

function createBaseVoteResult(): VoteResult {
  return { id: "", categoryId: "", submissionId: "", position: 0, title: undefined, createdAt: 0, modifiedAt: 0 };
}

export const VoteResult: MessageFns<VoteResult> = {
  encode(message: VoteResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.categoryId !== "") {
      writer.uint32(18).string(message.categoryId);
    }
    if (message.submissionId !== "") {
      writer.uint32(26).string(message.submissionId);
    }
    if (message.position !== 0) {
      writer.uint32(32).int32(message.position);
    }
    if (message.title !== undefined) {
      writer.uint32(42).string(message.title);
    }
    if (message.createdAt !== 0) {
      writer.uint32(48).int64(message.createdAt);
    }
    if (message.modifiedAt !== 0) {
      writer.uint32(56).int64(message.modifiedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoteResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoteResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.categoryId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.submissionId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.position = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.createdAt = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.modifiedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoteResult {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      categoryId: isSet(object.categoryId)
        ? globalThis.String(object.categoryId)
        : isSet(object.category_id)
        ? globalThis.String(object.category_id)
        : "",
      submissionId: isSet(object.submissionId)
        ? globalThis.String(object.submissionId)
        : isSet(object.submission_id)
        ? globalThis.String(object.submission_id)
        : "",
      position: isSet(object.position) ? globalThis.Number(object.position) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : undefined,
      createdAt: isSet(object.createdAt)
        ? globalThis.Number(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.Number(object.created_at)
        : 0,
      modifiedAt: isSet(object.modifiedAt)
        ? globalThis.Number(object.modifiedAt)
        : isSet(object.modified_at)
        ? globalThis.Number(object.modified_at)
        : 0,
    };
  },

  toJSON(message: VoteResult): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.categoryId !== "") {
      obj.categoryId = message.categoryId;
    }
    if (message.submissionId !== "") {
      obj.submissionId = message.submissionId;
    }
    if (message.position !== 0) {
      obj.position = Math.round(message.position);
    }
    if (message.title !== undefined) {
      obj.title = message.title;
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.modifiedAt !== 0) {
      obj.modifiedAt = Math.round(message.modifiedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<VoteResult>): VoteResult {
    return VoteResult.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VoteResult>): VoteResult {
    const message = createBaseVoteResult();
    message.id = object.id ?? "";
    message.categoryId = object.categoryId ?? "";
    message.submissionId = object.submissionId ?? "";
    message.position = object.position ?? 0;
    message.title = object.title ?? undefined;
    message.createdAt = object.createdAt ?? 0;
    message.modifiedAt = object.modifiedAt ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
