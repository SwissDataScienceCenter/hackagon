// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/hackathon_svc/revoke_invite_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hackathon.messages.hackathon_svc";

export interface RevokeInviteRequest {
  inviteId: string;
}

function createBaseRevokeInviteRequest(): RevokeInviteRequest {
  return { inviteId: "" };
}

export const RevokeInviteRequest: MessageFns<RevokeInviteRequest> = {
  encode(message: RevokeInviteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inviteId !== "") {
      writer.uint32(10).string(message.inviteId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeInviteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeInviteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.inviteId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeInviteRequest {
    return {
      inviteId: isSet(object.inviteId)
        ? globalThis.String(object.inviteId)
        : isSet(object.invite_id)
        ? globalThis.String(object.invite_id)
        : "",
    };
  },

  toJSON(message: RevokeInviteRequest): unknown {
    const obj: any = {};
    if (message.inviteId !== "") {
      obj.inviteId = message.inviteId;
    }
    return obj;
  },

  create(base?: DeepPartial<RevokeInviteRequest>): RevokeInviteRequest {
    return RevokeInviteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RevokeInviteRequest>): RevokeInviteRequest {
    const message = createBaseRevokeInviteRequest();
    message.inviteId = object.inviteId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
