// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/public_svc/list_hackathons_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PublicHackathonSummary } from "../../entities/public_hackathon";

export const protobufPackage = "hackathon.messages.public_svc";

export interface ListHackathonsResponse {
  hackathons: PublicHackathonSummary[];
}

function createBaseListHackathonsResponse(): ListHackathonsResponse {
  return { hackathons: [] };
}

export const ListHackathonsResponse: MessageFns<ListHackathonsResponse> = {
  encode(message: ListHackathonsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.hackathons) {
      PublicHackathonSummary.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListHackathonsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListHackathonsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hackathons.push(PublicHackathonSummary.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListHackathonsResponse {
    return {
      hackathons: globalThis.Array.isArray(object?.hackathons)
        ? object.hackathons.map((e: any) => PublicHackathonSummary.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListHackathonsResponse): unknown {
    const obj: any = {};
    if (message.hackathons?.length) {
      obj.hackathons = message.hackathons.map((e) => PublicHackathonSummary.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListHackathonsResponse>): ListHackathonsResponse {
    return ListHackathonsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListHackathonsResponse>): ListHackathonsResponse {
    const message = createBaseListHackathonsResponse();
    message.hackathons = object.hackathons?.map((e) => PublicHackathonSummary.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
