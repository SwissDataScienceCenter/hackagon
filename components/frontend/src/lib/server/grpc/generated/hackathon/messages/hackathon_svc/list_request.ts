// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/hackathon_svc/list_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { HackathonStatus, hackathonStatusFromJSON, hackathonStatusToJSON } from "../../entities/hackathon_status";
import { Visibility, visibilityFromJSON, visibilityToJSON } from "../../entities/visibility";

export const protobufPackage = "hackathon.messages.hackathon_svc";

export interface ListRequest {
  statusFilter: HackathonStatus[];
  ownerId?: string | undefined;
  participantId?: string | undefined;
  visibilityFilter?: Visibility | undefined;
}

function createBaseListRequest(): ListRequest {
  return { statusFilter: [], ownerId: undefined, participantId: undefined, visibilityFilter: undefined };
}

export const ListRequest: MessageFns<ListRequest> = {
  encode(message: ListRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.statusFilter) {
      writer.int32(v);
    }
    writer.join();
    if (message.ownerId !== undefined) {
      writer.uint32(18).string(message.ownerId);
    }
    if (message.participantId !== undefined) {
      writer.uint32(26).string(message.participantId);
    }
    if (message.visibilityFilter !== undefined) {
      writer.uint32(32).int32(message.visibilityFilter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.statusFilter.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.statusFilter.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.participantId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.visibilityFilter = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListRequest {
    return {
      statusFilter: globalThis.Array.isArray(object?.statusFilter)
        ? object.statusFilter.map((e: any) => hackathonStatusFromJSON(e))
        : globalThis.Array.isArray(object?.status_filter)
        ? object.status_filter.map((e: any) => hackathonStatusFromJSON(e))
        : [],
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : undefined,
      participantId: isSet(object.participantId)
        ? globalThis.String(object.participantId)
        : isSet(object.participant_id)
        ? globalThis.String(object.participant_id)
        : undefined,
      visibilityFilter: isSet(object.visibilityFilter)
        ? visibilityFromJSON(object.visibilityFilter)
        : isSet(object.visibility_filter)
        ? visibilityFromJSON(object.visibility_filter)
        : undefined,
    };
  },

  toJSON(message: ListRequest): unknown {
    const obj: any = {};
    if (message.statusFilter?.length) {
      obj.statusFilter = message.statusFilter.map((e) => hackathonStatusToJSON(e));
    }
    if (message.ownerId !== undefined) {
      obj.ownerId = message.ownerId;
    }
    if (message.participantId !== undefined) {
      obj.participantId = message.participantId;
    }
    if (message.visibilityFilter !== undefined) {
      obj.visibilityFilter = visibilityToJSON(message.visibilityFilter);
    }
    return obj;
  },

  create(base?: DeepPartial<ListRequest>): ListRequest {
    return ListRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListRequest>): ListRequest {
    const message = createBaseListRequest();
    message.statusFilter = object.statusFilter?.map((e) => e) || [];
    message.ownerId = object.ownerId ?? undefined;
    message.participantId = object.participantId ?? undefined;
    message.visibilityFilter = object.visibilityFilter ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
