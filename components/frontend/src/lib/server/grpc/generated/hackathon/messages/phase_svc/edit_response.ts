// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/phase_svc/edit_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Phase } from "../../entities/phase";

export const protobufPackage = "hackathon.messages.phase_svc";

export interface EditResponse {
  phase: Phase | undefined;
}

function createBaseEditResponse(): EditResponse {
  return { phase: undefined };
}

export const EditResponse: MessageFns<EditResponse> = {
  encode(message: EditResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.phase !== undefined) {
      Phase.encode(message.phase, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.phase = Phase.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EditResponse {
    return { phase: isSet(object.phase) ? Phase.fromJSON(object.phase) : undefined };
  },

  toJSON(message: EditResponse): unknown {
    const obj: any = {};
    if (message.phase !== undefined) {
      obj.phase = Phase.toJSON(message.phase);
    }
    return obj;
  },

  create(base?: DeepPartial<EditResponse>): EditResponse {
    return EditResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EditResponse>): EditResponse {
    const message = createBaseEditResponse();
    message.phase = (object.phase !== undefined && object.phase !== null) ? Phase.fromPartial(object.phase) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
