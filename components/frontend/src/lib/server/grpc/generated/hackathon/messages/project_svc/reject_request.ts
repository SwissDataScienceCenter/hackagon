// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/project_svc/reject_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hackathon.messages.project_svc";

export interface RejectRequest {
  projectId: string;
  reviewComment?: string | undefined;
}

function createBaseRejectRequest(): RejectRequest {
  return { projectId: "", reviewComment: undefined };
}

export const RejectRequest: MessageFns<RejectRequest> = {
  encode(message: RejectRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.reviewComment !== undefined) {
      writer.uint32(18).string(message.reviewComment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RejectRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRejectRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reviewComment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RejectRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      reviewComment: isSet(object.reviewComment)
        ? globalThis.String(object.reviewComment)
        : isSet(object.review_comment)
        ? globalThis.String(object.review_comment)
        : undefined,
    };
  },

  toJSON(message: RejectRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.reviewComment !== undefined) {
      obj.reviewComment = message.reviewComment;
    }
    return obj;
  },

  create(base?: DeepPartial<RejectRequest>): RejectRequest {
    return RejectRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RejectRequest>): RejectRequest {
    const message = createBaseRejectRequest();
    message.projectId = object.projectId ?? "";
    message.reviewComment = object.reviewComment ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
