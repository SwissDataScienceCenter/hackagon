// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/hackathon_svc/set_capabilities_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Capability, capabilityFromJSON, capabilityToJSON } from "../../entities/capability";

export const protobufPackage = "hackathon.messages.hackathon_svc";

export interface SetCapabilitiesRequest {
  hackathonId: string;
  capabilities: CapabilityState[];
}

export interface CapabilityState {
  capability: Capability;
  enabled: boolean;
}

function createBaseSetCapabilitiesRequest(): SetCapabilitiesRequest {
  return { hackathonId: "", capabilities: [] };
}

export const SetCapabilitiesRequest: MessageFns<SetCapabilitiesRequest> = {
  encode(message: SetCapabilitiesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hackathonId !== "") {
      writer.uint32(10).string(message.hackathonId);
    }
    for (const v of message.capabilities) {
      CapabilityState.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetCapabilitiesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetCapabilitiesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hackathonId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.capabilities.push(CapabilityState.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetCapabilitiesRequest {
    return {
      hackathonId: isSet(object.hackathonId)
        ? globalThis.String(object.hackathonId)
        : isSet(object.hackathon_id)
        ? globalThis.String(object.hackathon_id)
        : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => CapabilityState.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SetCapabilitiesRequest): unknown {
    const obj: any = {};
    if (message.hackathonId !== "") {
      obj.hackathonId = message.hackathonId;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => CapabilityState.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SetCapabilitiesRequest>): SetCapabilitiesRequest {
    return SetCapabilitiesRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetCapabilitiesRequest>): SetCapabilitiesRequest {
    const message = createBaseSetCapabilitiesRequest();
    message.hackathonId = object.hackathonId ?? "";
    message.capabilities = object.capabilities?.map((e) => CapabilityState.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityState(): CapabilityState {
  return { capability: 0, enabled: false };
}

export const CapabilityState: MessageFns<CapabilityState> = {
  encode(message: CapabilityState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capability !== 0) {
      writer.uint32(8).int32(message.capability);
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.capability = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityState {
    return {
      capability: isSet(object.capability) ? capabilityFromJSON(object.capability) : 0,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: CapabilityState): unknown {
    const obj: any = {};
    if (message.capability !== 0) {
      obj.capability = capabilityToJSON(message.capability);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<CapabilityState>): CapabilityState {
    return CapabilityState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CapabilityState>): CapabilityState {
    const message = createBaseCapabilityState();
    message.capability = object.capability ?? 0;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
