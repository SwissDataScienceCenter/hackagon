// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/hackathon.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { User } from "../../user/entities/user";
import { HackathonMember } from "./hackathon_member";
import { HackathonState } from "./hackathon_state";
import { HackathonStatus, hackathonStatusFromJSON, hackathonStatusToJSON } from "./hackathon_status";
import { Page } from "./page";
import { Phase } from "./phase";
import { Project } from "./project";
import { Track } from "./track";
import { Visibility, visibilityFromJSON, visibilityToJSON } from "./visibility";

export const protobufPackage = "hackathon.entities";

export interface Hackathon {
  id: string;
  name: string;
  startsAt?: Date | undefined;
  endsAt?: Date | undefined;
  createdAt: Date | undefined;
  modifiedAt: Date | undefined;
  visibility: Visibility;
  /** Computed server-side from starts_at/ends_at; not persisted in DB. */
  status: HackathonStatus;
  description?: string | undefined;
  logo?: string | undefined;
  members: HackathonMember[];
  creator: User | undefined;
  modifier:
    | User
    | undefined;
  /** The following collections are populated only on Get responses. */
  tracks: Track[];
  projects: Project[];
  pages: Page[];
  phases: Phase[];
  /**
   * Populated in List responses only when participant_id filter is set.
   * Contains the requesting user's membership in this hackathon (role + is_waiting).
   */
  viewerMembership?:
    | HackathonMember
    | undefined;
  /** Populated in Get responses only. */
  state:
    | HackathonState
    | undefined;
  /** Populated in Get responses only. */
  owners: User[];
}

function createBaseHackathon(): Hackathon {
  return {
    id: "",
    name: "",
    startsAt: undefined,
    endsAt: undefined,
    createdAt: undefined,
    modifiedAt: undefined,
    visibility: 0,
    status: 0,
    description: undefined,
    logo: undefined,
    members: [],
    creator: undefined,
    modifier: undefined,
    tracks: [],
    projects: [],
    pages: [],
    phases: [],
    viewerMembership: undefined,
    state: undefined,
    owners: [],
  };
}

export const Hackathon: MessageFns<Hackathon> = {
  encode(message: Hackathon, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.startsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startsAt), writer.uint32(26).fork()).join();
    }
    if (message.endsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endsAt), writer.uint32(34).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    if (message.modifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAt), writer.uint32(50).fork()).join();
    }
    if (message.visibility !== 0) {
      writer.uint32(56).int32(message.visibility);
    }
    if (message.status !== 0) {
      writer.uint32(64).int32(message.status);
    }
    if (message.description !== undefined) {
      writer.uint32(74).string(message.description);
    }
    if (message.logo !== undefined) {
      writer.uint32(82).string(message.logo);
    }
    for (const v of message.members) {
      HackathonMember.encode(v!, writer.uint32(90).fork()).join();
    }
    if (message.creator !== undefined) {
      User.encode(message.creator, writer.uint32(98).fork()).join();
    }
    if (message.modifier !== undefined) {
      User.encode(message.modifier, writer.uint32(106).fork()).join();
    }
    for (const v of message.tracks) {
      Track.encode(v!, writer.uint32(114).fork()).join();
    }
    for (const v of message.projects) {
      Project.encode(v!, writer.uint32(122).fork()).join();
    }
    for (const v of message.pages) {
      Page.encode(v!, writer.uint32(130).fork()).join();
    }
    for (const v of message.phases) {
      Phase.encode(v!, writer.uint32(138).fork()).join();
    }
    if (message.viewerMembership !== undefined) {
      HackathonMember.encode(message.viewerMembership, writer.uint32(146).fork()).join();
    }
    if (message.state !== undefined) {
      HackathonState.encode(message.state, writer.uint32(154).fork()).join();
    }
    for (const v of message.owners) {
      User.encode(v!, writer.uint32(162).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hackathon {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHackathon();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.modifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.visibility = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.logo = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.members.push(HackathonMember.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.creator = User.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.modifier = User.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.tracks.push(Track.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.projects.push(Project.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.pages.push(Page.decode(reader, reader.uint32()));
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.phases.push(Phase.decode(reader, reader.uint32()));
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.viewerMembership = HackathonMember.decode(reader, reader.uint32());
          continue;
        }
        case 19: {
          if (tag !== 154) {
            break;
          }

          message.state = HackathonState.decode(reader, reader.uint32());
          continue;
        }
        case 20: {
          if (tag !== 162) {
            break;
          }

          message.owners.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hackathon {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      startsAt: isSet(object.startsAt)
        ? fromJsonTimestamp(object.startsAt)
        : isSet(object.starts_at)
        ? fromJsonTimestamp(object.starts_at)
        : undefined,
      endsAt: isSet(object.endsAt)
        ? fromJsonTimestamp(object.endsAt)
        : isSet(object.ends_at)
        ? fromJsonTimestamp(object.ends_at)
        : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      modifiedAt: isSet(object.modifiedAt)
        ? fromJsonTimestamp(object.modifiedAt)
        : isSet(object.modified_at)
        ? fromJsonTimestamp(object.modified_at)
        : undefined,
      visibility: isSet(object.visibility) ? visibilityFromJSON(object.visibility) : 0,
      status: isSet(object.status) ? hackathonStatusFromJSON(object.status) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      logo: isSet(object.logo) ? globalThis.String(object.logo) : undefined,
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => HackathonMember.fromJSON(e))
        : [],
      creator: isSet(object.creator) ? User.fromJSON(object.creator) : undefined,
      modifier: isSet(object.modifier) ? User.fromJSON(object.modifier) : undefined,
      tracks: globalThis.Array.isArray(object?.tracks)
        ? object.tracks.map((e: any) => Track.fromJSON(e))
        : [],
      projects: globalThis.Array.isArray(object?.projects) ? object.projects.map((e: any) => Project.fromJSON(e)) : [],
      pages: globalThis.Array.isArray(object?.pages) ? object.pages.map((e: any) => Page.fromJSON(e)) : [],
      phases: globalThis.Array.isArray(object?.phases) ? object.phases.map((e: any) => Phase.fromJSON(e)) : [],
      viewerMembership: isSet(object.viewerMembership)
        ? HackathonMember.fromJSON(object.viewerMembership)
        : isSet(object.viewer_membership)
        ? HackathonMember.fromJSON(object.viewer_membership)
        : undefined,
      state: isSet(object.state) ? HackathonState.fromJSON(object.state) : undefined,
      owners: globalThis.Array.isArray(object?.owners) ? object.owners.map((e: any) => User.fromJSON(e)) : [],
    };
  },

  toJSON(message: Hackathon): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.startsAt !== undefined) {
      obj.startsAt = message.startsAt.toISOString();
    }
    if (message.endsAt !== undefined) {
      obj.endsAt = message.endsAt.toISOString();
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.modifiedAt !== undefined) {
      obj.modifiedAt = message.modifiedAt.toISOString();
    }
    if (message.visibility !== 0) {
      obj.visibility = visibilityToJSON(message.visibility);
    }
    if (message.status !== 0) {
      obj.status = hackathonStatusToJSON(message.status);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.logo !== undefined) {
      obj.logo = message.logo;
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => HackathonMember.toJSON(e));
    }
    if (message.creator !== undefined) {
      obj.creator = User.toJSON(message.creator);
    }
    if (message.modifier !== undefined) {
      obj.modifier = User.toJSON(message.modifier);
    }
    if (message.tracks?.length) {
      obj.tracks = message.tracks.map((e) => Track.toJSON(e));
    }
    if (message.projects?.length) {
      obj.projects = message.projects.map((e) => Project.toJSON(e));
    }
    if (message.pages?.length) {
      obj.pages = message.pages.map((e) => Page.toJSON(e));
    }
    if (message.phases?.length) {
      obj.phases = message.phases.map((e) => Phase.toJSON(e));
    }
    if (message.viewerMembership !== undefined) {
      obj.viewerMembership = HackathonMember.toJSON(message.viewerMembership);
    }
    if (message.state !== undefined) {
      obj.state = HackathonState.toJSON(message.state);
    }
    if (message.owners?.length) {
      obj.owners = message.owners.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Hackathon>): Hackathon {
    return Hackathon.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Hackathon>): Hackathon {
    const message = createBaseHackathon();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.startsAt = object.startsAt ?? undefined;
    message.endsAt = object.endsAt ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.modifiedAt = object.modifiedAt ?? undefined;
    message.visibility = object.visibility ?? 0;
    message.status = object.status ?? 0;
    message.description = object.description ?? undefined;
    message.logo = object.logo ?? undefined;
    message.members = object.members?.map((e) => HackathonMember.fromPartial(e)) || [];
    message.creator = (object.creator !== undefined && object.creator !== null)
      ? User.fromPartial(object.creator)
      : undefined;
    message.modifier = (object.modifier !== undefined && object.modifier !== null)
      ? User.fromPartial(object.modifier)
      : undefined;
    message.tracks = object.tracks?.map((e) => Track.fromPartial(e)) || [];
    message.projects = object.projects?.map((e) => Project.fromPartial(e)) || [];
    message.pages = object.pages?.map((e) => Page.fromPartial(e)) || [];
    message.phases = object.phases?.map((e) => Phase.fromPartial(e)) || [];
    message.viewerMembership = (object.viewerMembership !== undefined && object.viewerMembership !== null)
      ? HackathonMember.fromPartial(object.viewerMembership)
      : undefined;
    message.state = (object.state !== undefined && object.state !== null)
      ? HackathonState.fromPartial(object.state)
      : undefined;
    message.owners = object.owners?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
