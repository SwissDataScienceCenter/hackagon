// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/page_svc/set_order_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hackathon.messages.page_svc";

export interface SetOrderRequest {
  hackathonId: string;
  /** List of page IDs in the desired order */
  pageIds: string[];
}

function createBaseSetOrderRequest(): SetOrderRequest {
  return { hackathonId: "", pageIds: [] };
}

export const SetOrderRequest: MessageFns<SetOrderRequest> = {
  encode(message: SetOrderRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hackathonId !== "") {
      writer.uint32(10).string(message.hackathonId);
    }
    for (const v of message.pageIds) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetOrderRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetOrderRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hackathonId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pageIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetOrderRequest {
    return {
      hackathonId: isSet(object.hackathonId)
        ? globalThis.String(object.hackathonId)
        : isSet(object.hackathon_id)
        ? globalThis.String(object.hackathon_id)
        : "",
      pageIds: globalThis.Array.isArray(object?.pageIds)
        ? object.pageIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.page_ids)
        ? object.page_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SetOrderRequest): unknown {
    const obj: any = {};
    if (message.hackathonId !== "") {
      obj.hackathonId = message.hackathonId;
    }
    if (message.pageIds?.length) {
      obj.pageIds = message.pageIds;
    }
    return obj;
  },

  create(base?: DeepPartial<SetOrderRequest>): SetOrderRequest {
    return SetOrderRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetOrderRequest>): SetOrderRequest {
    const message = createBaseSetOrderRequest();
    message.hackathonId = object.hackathonId ?? "";
    message.pageIds = object.pageIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
