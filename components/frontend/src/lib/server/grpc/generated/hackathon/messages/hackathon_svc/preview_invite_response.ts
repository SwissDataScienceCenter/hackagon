// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/hackathon_svc/preview_invite_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Hackathon } from "../../entities/hackathon";
import { Question } from "../../entities/question";

export const protobufPackage = "hackathon.messages.hackathon_svc";

export interface PreviewInviteResponse {
  hackathon: Hackathon | undefined;
  questions: Question[];
  alreadyParticipant: boolean;
}

function createBasePreviewInviteResponse(): PreviewInviteResponse {
  return { hackathon: undefined, questions: [], alreadyParticipant: false };
}

export const PreviewInviteResponse: MessageFns<PreviewInviteResponse> = {
  encode(message: PreviewInviteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hackathon !== undefined) {
      Hackathon.encode(message.hackathon, writer.uint32(10).fork()).join();
    }
    for (const v of message.questions) {
      Question.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.alreadyParticipant !== false) {
      writer.uint32(24).bool(message.alreadyParticipant);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreviewInviteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePreviewInviteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hackathon = Hackathon.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.questions.push(Question.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.alreadyParticipant = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PreviewInviteResponse {
    return {
      hackathon: isSet(object.hackathon) ? Hackathon.fromJSON(object.hackathon) : undefined,
      questions: globalThis.Array.isArray(object?.questions)
        ? object.questions.map((e: any) => Question.fromJSON(e))
        : [],
      alreadyParticipant: isSet(object.alreadyParticipant)
        ? globalThis.Boolean(object.alreadyParticipant)
        : isSet(object.already_participant)
        ? globalThis.Boolean(object.already_participant)
        : false,
    };
  },

  toJSON(message: PreviewInviteResponse): unknown {
    const obj: any = {};
    if (message.hackathon !== undefined) {
      obj.hackathon = Hackathon.toJSON(message.hackathon);
    }
    if (message.questions?.length) {
      obj.questions = message.questions.map((e) => Question.toJSON(e));
    }
    if (message.alreadyParticipant !== false) {
      obj.alreadyParticipant = message.alreadyParticipant;
    }
    return obj;
  },

  create(base?: DeepPartial<PreviewInviteResponse>): PreviewInviteResponse {
    return PreviewInviteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PreviewInviteResponse>): PreviewInviteResponse {
    const message = createBasePreviewInviteResponse();
    message.hackathon = (object.hackathon !== undefined && object.hackathon !== null)
      ? Hackathon.fromPartial(object.hackathon)
      : undefined;
    message.questions = object.questions?.map((e) => Question.fromPartial(e)) || [];
    message.alreadyParticipant = object.alreadyParticipant ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
