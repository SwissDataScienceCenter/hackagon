// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/team_svc/get_submission_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Submission } from "../../entities/submission";

export const protobufPackage = "hackathon.messages.team_svc";

export interface GetSubmissionResponse {
  submission: Submission | undefined;
}

function createBaseGetSubmissionResponse(): GetSubmissionResponse {
  return { submission: undefined };
}

export const GetSubmissionResponse: MessageFns<GetSubmissionResponse> = {
  encode(message: GetSubmissionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.submission !== undefined) {
      Submission.encode(message.submission, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetSubmissionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetSubmissionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.submission = Submission.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetSubmissionResponse {
    return { submission: isSet(object.submission) ? Submission.fromJSON(object.submission) : undefined };
  },

  toJSON(message: GetSubmissionResponse): unknown {
    const obj: any = {};
    if (message.submission !== undefined) {
      obj.submission = Submission.toJSON(message.submission);
    }
    return obj;
  },

  create(base?: DeepPartial<GetSubmissionResponse>): GetSubmissionResponse {
    return GetSubmissionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetSubmissionResponse>): GetSubmissionResponse {
    const message = createBaseGetSubmissionResponse();
    message.submission = (object.submission !== undefined && object.submission !== null)
      ? Submission.fromPartial(object.submission)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
