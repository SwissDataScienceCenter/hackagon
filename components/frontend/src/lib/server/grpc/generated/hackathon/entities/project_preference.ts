// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/project_preference.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { User } from "../../user/entities/user";
import { ProjectStatus, projectStatusFromJSON, projectStatusToJSON } from "./project_status";

export const protobufPackage = "hackathon.entities";

export interface ProjectWithPreferences {
  id: string;
  title: string;
  description: string;
  status: ProjectStatus;
  image?: string | undefined;
  createdAt: Date | undefined;
  modifiedAt: Date | undefined;
  trackId: string;
  hackathonId: string;
  /** List of users who have this project as their preference */
  preferences: User[];
}

function createBaseProjectWithPreferences(): ProjectWithPreferences {
  return {
    id: "",
    title: "",
    description: "",
    status: 0,
    image: undefined,
    createdAt: undefined,
    modifiedAt: undefined,
    trackId: "",
    hackathonId: "",
    preferences: [],
  };
}

export const ProjectWithPreferences: MessageFns<ProjectWithPreferences> = {
  encode(message: ProjectWithPreferences, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.image !== undefined) {
      writer.uint32(42).string(message.image);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(50).fork()).join();
    }
    if (message.modifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAt), writer.uint32(58).fork()).join();
    }
    if (message.trackId !== "") {
      writer.uint32(66).string(message.trackId);
    }
    if (message.hackathonId !== "") {
      writer.uint32(74).string(message.hackathonId);
    }
    for (const v of message.preferences) {
      User.encode(v!, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProjectWithPreferences {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProjectWithPreferences();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.image = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.modifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.trackId = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.hackathonId = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.preferences.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProjectWithPreferences {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      status: isSet(object.status) ? projectStatusFromJSON(object.status) : 0,
      image: isSet(object.image) ? globalThis.String(object.image) : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      modifiedAt: isSet(object.modifiedAt)
        ? fromJsonTimestamp(object.modifiedAt)
        : isSet(object.modified_at)
        ? fromJsonTimestamp(object.modified_at)
        : undefined,
      trackId: isSet(object.trackId)
        ? globalThis.String(object.trackId)
        : isSet(object.track_id)
        ? globalThis.String(object.track_id)
        : "",
      hackathonId: isSet(object.hackathonId)
        ? globalThis.String(object.hackathonId)
        : isSet(object.hackathon_id)
        ? globalThis.String(object.hackathon_id)
        : "",
      preferences: globalThis.Array.isArray(object?.preferences)
        ? object.preferences.map((e: any) => User.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ProjectWithPreferences): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.status !== 0) {
      obj.status = projectStatusToJSON(message.status);
    }
    if (message.image !== undefined) {
      obj.image = message.image;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.modifiedAt !== undefined) {
      obj.modifiedAt = message.modifiedAt.toISOString();
    }
    if (message.trackId !== "") {
      obj.trackId = message.trackId;
    }
    if (message.hackathonId !== "") {
      obj.hackathonId = message.hackathonId;
    }
    if (message.preferences?.length) {
      obj.preferences = message.preferences.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ProjectWithPreferences>): ProjectWithPreferences {
    return ProjectWithPreferences.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ProjectWithPreferences>): ProjectWithPreferences {
    const message = createBaseProjectWithPreferences();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.status = object.status ?? 0;
    message.image = object.image ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    message.modifiedAt = object.modifiedAt ?? undefined;
    message.trackId = object.trackId ?? "";
    message.hackathonId = object.hackathonId ?? "";
    message.preferences = object.preferences?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
