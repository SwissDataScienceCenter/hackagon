// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/public_team.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PublicSubmission } from "./public_submission";

export const protobufPackage = "hackathon.entities";

/**
 * A team as the public sees it: a member_count rather than members.
 * See PublicHackathon for why this entity exists.
 */
export interface PublicTeam {
  id: string;
  name: string;
  description?: string | undefined;
  projectId: string;
  memberCount: number;
  /** Final submissions only; see PublicSubmission. */
  submissions: PublicSubmission[];
}

function createBasePublicTeam(): PublicTeam {
  return { id: "", name: "", description: undefined, projectId: "", memberCount: 0, submissions: [] };
}

export const PublicTeam: MessageFns<PublicTeam> = {
  encode(message: PublicTeam, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.projectId !== "") {
      writer.uint32(34).string(message.projectId);
    }
    if (message.memberCount !== 0) {
      writer.uint32(40).int32(message.memberCount);
    }
    for (const v of message.submissions) {
      PublicSubmission.encode(v!, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublicTeam {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublicTeam();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.memberCount = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.submissions.push(PublicSubmission.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublicTeam {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      memberCount: isSet(object.memberCount)
        ? globalThis.Number(object.memberCount)
        : isSet(object.member_count)
        ? globalThis.Number(object.member_count)
        : 0,
      submissions: globalThis.Array.isArray(object?.submissions)
        ? object.submissions.map((e: any) => PublicSubmission.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PublicTeam): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.memberCount !== 0) {
      obj.memberCount = Math.round(message.memberCount);
    }
    if (message.submissions?.length) {
      obj.submissions = message.submissions.map((e) => PublicSubmission.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PublicTeam>): PublicTeam {
    return PublicTeam.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublicTeam>): PublicTeam {
    const message = createBasePublicTeam();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? undefined;
    message.projectId = object.projectId ?? "";
    message.memberCount = object.memberCount ?? 0;
    message.submissions = object.submissions?.map((e) => PublicSubmission.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
