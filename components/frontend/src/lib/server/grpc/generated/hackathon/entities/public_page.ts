// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/public_page.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hackathon.entities";

/**
 * A content page as the public sees it. Carries no author and no timestamps.
 * Only pages with visible = true ever become one of these.
 * See PublicHackathon for why this entity exists.
 */
export interface PublicPage {
  id: string;
  title: string;
  content: string;
  order: number;
  phaseId?: string | undefined;
}

function createBasePublicPage(): PublicPage {
  return { id: "", title: "", content: "", order: 0, phaseId: undefined };
}

export const PublicPage: MessageFns<PublicPage> = {
  encode(message: PublicPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    if (message.order !== 0) {
      writer.uint32(32).int32(message.order);
    }
    if (message.phaseId !== undefined) {
      writer.uint32(42).string(message.phaseId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublicPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublicPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.order = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.phaseId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublicPage {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      order: isSet(object.order) ? globalThis.Number(object.order) : 0,
      phaseId: isSet(object.phaseId)
        ? globalThis.String(object.phaseId)
        : isSet(object.phase_id)
        ? globalThis.String(object.phase_id)
        : undefined,
    };
  },

  toJSON(message: PublicPage): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.order !== 0) {
      obj.order = Math.round(message.order);
    }
    if (message.phaseId !== undefined) {
      obj.phaseId = message.phaseId;
    }
    return obj;
  },

  create(base?: DeepPartial<PublicPage>): PublicPage {
    return PublicPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublicPage>): PublicPage {
    const message = createBasePublicPage();
    message.id = object.id ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.order = object.order ?? 0;
    message.phaseId = object.phaseId ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
