// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/page_svc/set_order_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "hackathon.messages.page_svc";

export interface SetOrderResponse {
  pageIds: string[];
}

function createBaseSetOrderResponse(): SetOrderResponse {
  return { pageIds: [] };
}

export const SetOrderResponse: MessageFns<SetOrderResponse> = {
  encode(message: SetOrderResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.pageIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetOrderResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetOrderResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pageIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetOrderResponse {
    return {
      pageIds: globalThis.Array.isArray(object?.pageIds)
        ? object.pageIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.page_ids)
        ? object.page_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: SetOrderResponse): unknown {
    const obj: any = {};
    if (message.pageIds?.length) {
      obj.pageIds = message.pageIds;
    }
    return obj;
  },

  create(base?: DeepPartial<SetOrderResponse>): SetOrderResponse {
    return SetOrderResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetOrderResponse>): SetOrderResponse {
    const message = createBaseSetOrderResponse();
    message.pageIds = object.pageIds?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
