// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/public_hackathon.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { HackathonStatus, hackathonStatusFromJSON, hackathonStatusToJSON } from "./hackathon_status";
import { PublicPage } from "./public_page";
import { PublicPhase } from "./public_phase";
import { PublicProject } from "./public_project";
import { PublicTeam } from "./public_team";
import { PublicTrack } from "./public_track";

export const protobufPackage = "hackathon.entities";

/**
 * The public view of one hackathon: everything a visitor with no account may
 * see, and nothing else.
 *
 * These Public* messages exist as a family separate from Hackathon, Team,
 * Project and the rest for one reason to avoid leaking user information so to be
 * sure that the site complies with GDPR.
 */
export interface PublicHackathon {
  id: string;
  name: string;
  description: string;
  logo?: string | undefined;
  startsAt?: Date | undefined;
  endsAt?:
    | Date
    | undefined;
  /** Computed server-side from the dates, as on the participant-facing message. */
  status: HackathonStatus;
  /** Only pages the organizers have made visible. */
  pages: PublicPage[];
  tracks: PublicTrack[];
  phases: PublicPhase[];
  /** Approved projects only — a proposal nobody has accepted is not public. */
  projects: PublicProject[];
  teams: PublicTeam[];
}

/**
 * One row of the public hackathon directory.
 * Deliberately not PublicHackathon with the collections left empty
 */
export interface PublicHackathonSummary {
  id: string;
  name: string;
  description: string;
  logo?: string | undefined;
  startsAt?: Date | undefined;
  endsAt?: Date | undefined;
  status: HackathonStatus;
}

function createBasePublicHackathon(): PublicHackathon {
  return {
    id: "",
    name: "",
    description: "",
    logo: undefined,
    startsAt: undefined,
    endsAt: undefined,
    status: 0,
    pages: [],
    tracks: [],
    phases: [],
    projects: [],
    teams: [],
  };
}

export const PublicHackathon: MessageFns<PublicHackathon> = {
  encode(message: PublicHackathon, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.logo !== undefined) {
      writer.uint32(34).string(message.logo);
    }
    if (message.startsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startsAt), writer.uint32(42).fork()).join();
    }
    if (message.endsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endsAt), writer.uint32(50).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    for (const v of message.pages) {
      PublicPage.encode(v!, writer.uint32(66).fork()).join();
    }
    for (const v of message.tracks) {
      PublicTrack.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.phases) {
      PublicPhase.encode(v!, writer.uint32(82).fork()).join();
    }
    for (const v of message.projects) {
      PublicProject.encode(v!, writer.uint32(90).fork()).join();
    }
    for (const v of message.teams) {
      PublicTeam.encode(v!, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublicHackathon {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublicHackathon();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.logo = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.pages.push(PublicPage.decode(reader, reader.uint32()));
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.tracks.push(PublicTrack.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.phases.push(PublicPhase.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.projects.push(PublicProject.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.teams.push(PublicTeam.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublicHackathon {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      logo: isSet(object.logo) ? globalThis.String(object.logo) : undefined,
      startsAt: isSet(object.startsAt)
        ? fromJsonTimestamp(object.startsAt)
        : isSet(object.starts_at)
        ? fromJsonTimestamp(object.starts_at)
        : undefined,
      endsAt: isSet(object.endsAt)
        ? fromJsonTimestamp(object.endsAt)
        : isSet(object.ends_at)
        ? fromJsonTimestamp(object.ends_at)
        : undefined,
      status: isSet(object.status) ? hackathonStatusFromJSON(object.status) : 0,
      pages: globalThis.Array.isArray(object?.pages) ? object.pages.map((e: any) => PublicPage.fromJSON(e)) : [],
      tracks: globalThis.Array.isArray(object?.tracks) ? object.tracks.map((e: any) => PublicTrack.fromJSON(e)) : [],
      phases: globalThis.Array.isArray(object?.phases) ? object.phases.map((e: any) => PublicPhase.fromJSON(e)) : [],
      projects: globalThis.Array.isArray(object?.projects)
        ? object.projects.map((e: any) => PublicProject.fromJSON(e))
        : [],
      teams: globalThis.Array.isArray(object?.teams)
        ? object.teams.map((e: any) => PublicTeam.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PublicHackathon): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.logo !== undefined) {
      obj.logo = message.logo;
    }
    if (message.startsAt !== undefined) {
      obj.startsAt = message.startsAt.toISOString();
    }
    if (message.endsAt !== undefined) {
      obj.endsAt = message.endsAt.toISOString();
    }
    if (message.status !== 0) {
      obj.status = hackathonStatusToJSON(message.status);
    }
    if (message.pages?.length) {
      obj.pages = message.pages.map((e) => PublicPage.toJSON(e));
    }
    if (message.tracks?.length) {
      obj.tracks = message.tracks.map((e) => PublicTrack.toJSON(e));
    }
    if (message.phases?.length) {
      obj.phases = message.phases.map((e) => PublicPhase.toJSON(e));
    }
    if (message.projects?.length) {
      obj.projects = message.projects.map((e) => PublicProject.toJSON(e));
    }
    if (message.teams?.length) {
      obj.teams = message.teams.map((e) => PublicTeam.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PublicHackathon>): PublicHackathon {
    return PublicHackathon.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublicHackathon>): PublicHackathon {
    const message = createBasePublicHackathon();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.logo = object.logo ?? undefined;
    message.startsAt = object.startsAt ?? undefined;
    message.endsAt = object.endsAt ?? undefined;
    message.status = object.status ?? 0;
    message.pages = object.pages?.map((e) => PublicPage.fromPartial(e)) || [];
    message.tracks = object.tracks?.map((e) => PublicTrack.fromPartial(e)) || [];
    message.phases = object.phases?.map((e) => PublicPhase.fromPartial(e)) || [];
    message.projects = object.projects?.map((e) => PublicProject.fromPartial(e)) || [];
    message.teams = object.teams?.map((e) => PublicTeam.fromPartial(e)) || [];
    return message;
  },
};

function createBasePublicHackathonSummary(): PublicHackathonSummary {
  return { id: "", name: "", description: "", logo: undefined, startsAt: undefined, endsAt: undefined, status: 0 };
}

export const PublicHackathonSummary: MessageFns<PublicHackathonSummary> = {
  encode(message: PublicHackathonSummary, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.logo !== undefined) {
      writer.uint32(34).string(message.logo);
    }
    if (message.startsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startsAt), writer.uint32(42).fork()).join();
    }
    if (message.endsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endsAt), writer.uint32(50).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublicHackathonSummary {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublicHackathonSummary();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.logo = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublicHackathonSummary {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      logo: isSet(object.logo) ? globalThis.String(object.logo) : undefined,
      startsAt: isSet(object.startsAt)
        ? fromJsonTimestamp(object.startsAt)
        : isSet(object.starts_at)
        ? fromJsonTimestamp(object.starts_at)
        : undefined,
      endsAt: isSet(object.endsAt)
        ? fromJsonTimestamp(object.endsAt)
        : isSet(object.ends_at)
        ? fromJsonTimestamp(object.ends_at)
        : undefined,
      status: isSet(object.status) ? hackathonStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: PublicHackathonSummary): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.logo !== undefined) {
      obj.logo = message.logo;
    }
    if (message.startsAt !== undefined) {
      obj.startsAt = message.startsAt.toISOString();
    }
    if (message.endsAt !== undefined) {
      obj.endsAt = message.endsAt.toISOString();
    }
    if (message.status !== 0) {
      obj.status = hackathonStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<PublicHackathonSummary>): PublicHackathonSummary {
    return PublicHackathonSummary.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublicHackathonSummary>): PublicHackathonSummary {
    const message = createBasePublicHackathonSummary();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.logo = object.logo ?? undefined;
    message.startsAt = object.startsAt ?? undefined;
    message.endsAt = object.endsAt ?? undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
