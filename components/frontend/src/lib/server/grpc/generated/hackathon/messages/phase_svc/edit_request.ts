// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/phase_svc/edit_request.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { Capability, capabilityFromJSON, capabilityToJSON } from "../../entities/capability";

export const protobufPackage = "hackathon.messages.phase_svc";

export interface EditRequest {
  phaseId: string;
  name?: string | undefined;
  description?: string | undefined;
  startsAt?: Date | undefined;
  endsAt?:
    | Date
    | undefined;
  /** Empty string = unlink (unset), non-empty string = link to this phase, not set = no change */
  pageId?: string | undefined;
  capabilities?: EditRequest_PhaseCapabilities | undefined;
}

export interface EditRequest_PhaseCapabilities {
  items: Capability[];
}

function createBaseEditRequest(): EditRequest {
  return {
    phaseId: "",
    name: undefined,
    description: undefined,
    startsAt: undefined,
    endsAt: undefined,
    pageId: undefined,
    capabilities: undefined,
  };
}

export const EditRequest: MessageFns<EditRequest> = {
  encode(message: EditRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.phaseId !== "") {
      writer.uint32(10).string(message.phaseId);
    }
    if (message.name !== undefined) {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== undefined) {
      writer.uint32(26).string(message.description);
    }
    if (message.startsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startsAt), writer.uint32(34).fork()).join();
    }
    if (message.endsAt !== undefined) {
      Timestamp.encode(toTimestamp(message.endsAt), writer.uint32(42).fork()).join();
    }
    if (message.pageId !== undefined) {
      writer.uint32(50).string(message.pageId);
    }
    if (message.capabilities !== undefined) {
      EditRequest_PhaseCapabilities.encode(message.capabilities, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.phaseId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endsAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.pageId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.capabilities = EditRequest_PhaseCapabilities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EditRequest {
    return {
      phaseId: isSet(object.phaseId)
        ? globalThis.String(object.phaseId)
        : isSet(object.phase_id)
        ? globalThis.String(object.phase_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : undefined,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      startsAt: isSet(object.startsAt)
        ? fromJsonTimestamp(object.startsAt)
        : isSet(object.starts_at)
        ? fromJsonTimestamp(object.starts_at)
        : undefined,
      endsAt: isSet(object.endsAt)
        ? fromJsonTimestamp(object.endsAt)
        : isSet(object.ends_at)
        ? fromJsonTimestamp(object.ends_at)
        : undefined,
      pageId: isSet(object.pageId)
        ? globalThis.String(object.pageId)
        : isSet(object.page_id)
        ? globalThis.String(object.page_id)
        : undefined,
      capabilities: isSet(object.capabilities)
        ? EditRequest_PhaseCapabilities.fromJSON(object.capabilities)
        : undefined,
    };
  },

  toJSON(message: EditRequest): unknown {
    const obj: any = {};
    if (message.phaseId !== "") {
      obj.phaseId = message.phaseId;
    }
    if (message.name !== undefined) {
      obj.name = message.name;
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.startsAt !== undefined) {
      obj.startsAt = message.startsAt.toISOString();
    }
    if (message.endsAt !== undefined) {
      obj.endsAt = message.endsAt.toISOString();
    }
    if (message.pageId !== undefined) {
      obj.pageId = message.pageId;
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = EditRequest_PhaseCapabilities.toJSON(message.capabilities);
    }
    return obj;
  },

  create(base?: DeepPartial<EditRequest>): EditRequest {
    return EditRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EditRequest>): EditRequest {
    const message = createBaseEditRequest();
    message.phaseId = object.phaseId ?? "";
    message.name = object.name ?? undefined;
    message.description = object.description ?? undefined;
    message.startsAt = object.startsAt ?? undefined;
    message.endsAt = object.endsAt ?? undefined;
    message.pageId = object.pageId ?? undefined;
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? EditRequest_PhaseCapabilities.fromPartial(object.capabilities)
      : undefined;
    return message;
  },
};

function createBaseEditRequest_PhaseCapabilities(): EditRequest_PhaseCapabilities {
  return { items: [] };
}

export const EditRequest_PhaseCapabilities: MessageFns<EditRequest_PhaseCapabilities> = {
  encode(message: EditRequest_PhaseCapabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      writer.uint32(8).int32(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EditRequest_PhaseCapabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEditRequest_PhaseCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.items.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.items.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EditRequest_PhaseCapabilities {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => capabilityFromJSON(e)) : [],
    };
  },

  toJSON(message: EditRequest_PhaseCapabilities): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => capabilityToJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<EditRequest_PhaseCapabilities>): EditRequest_PhaseCapabilities {
    return EditRequest_PhaseCapabilities.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EditRequest_PhaseCapabilities>): EditRequest_PhaseCapabilities {
    const message = createBaseEditRequest_PhaseCapabilities();
    message.items = object.items?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
