// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/messages/hackathon_svc/list_invites_response.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { HackathonInvite } from "../../entities/hackathon_invite";

export const protobufPackage = "hackathon.messages.hackathon_svc";

export interface ListInvitesResponse {
  invites: HackathonInvite[];
}

function createBaseListInvitesResponse(): ListInvitesResponse {
  return { invites: [] };
}

export const ListInvitesResponse: MessageFns<ListInvitesResponse> = {
  encode(message: ListInvitesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.invites) {
      HackathonInvite.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListInvitesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListInvitesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.invites.push(HackathonInvite.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListInvitesResponse {
    return {
      invites: globalThis.Array.isArray(object?.invites)
        ? object.invites.map((e: any) => HackathonInvite.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListInvitesResponse): unknown {
    const obj: any = {};
    if (message.invites?.length) {
      obj.invites = message.invites.map((e) => HackathonInvite.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListInvitesResponse>): ListInvitesResponse {
    return ListInvitesResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListInvitesResponse>): ListInvitesResponse {
    const message = createBaseListInvitesResponse();
    message.invites = object.invites?.map((e) => HackathonInvite.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
