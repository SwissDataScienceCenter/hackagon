// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/hackathon_state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Capability, capabilityFromJSON, capabilityToJSON } from "./capability";

export const protobufPackage = "hackathon.entities";

export interface HackathonState {
  id: string;
  createdAt: Date | undefined;
  modifiedAt: Date | undefined;
  currentPhaseId: string;
  capabilities: CapabilityState[];
}

export interface CapabilityState {
  capability: Capability;
  enabled: boolean;
}

function createBaseHackathonState(): HackathonState {
  return { id: "", createdAt: undefined, modifiedAt: undefined, currentPhaseId: "", capabilities: [] };
}

export const HackathonState: MessageFns<HackathonState> = {
  encode(message: HackathonState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(18).fork()).join();
    }
    if (message.modifiedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.modifiedAt), writer.uint32(26).fork()).join();
    }
    if (message.currentPhaseId !== "") {
      writer.uint32(34).string(message.currentPhaseId);
    }
    for (const v of message.capabilities) {
      CapabilityState.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HackathonState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHackathonState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.modifiedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.currentPhaseId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.capabilities.push(CapabilityState.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HackathonState {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
      modifiedAt: isSet(object.modifiedAt)
        ? fromJsonTimestamp(object.modifiedAt)
        : isSet(object.modified_at)
        ? fromJsonTimestamp(object.modified_at)
        : undefined,
      currentPhaseId: isSet(object.currentPhaseId)
        ? globalThis.String(object.currentPhaseId)
        : isSet(object.current_phase_id)
        ? globalThis.String(object.current_phase_id)
        : "",
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => CapabilityState.fromJSON(e))
        : [],
    };
  },

  toJSON(message: HackathonState): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    if (message.modifiedAt !== undefined) {
      obj.modifiedAt = message.modifiedAt.toISOString();
    }
    if (message.currentPhaseId !== "") {
      obj.currentPhaseId = message.currentPhaseId;
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => CapabilityState.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<HackathonState>): HackathonState {
    return HackathonState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HackathonState>): HackathonState {
    const message = createBaseHackathonState();
    message.id = object.id ?? "";
    message.createdAt = object.createdAt ?? undefined;
    message.modifiedAt = object.modifiedAt ?? undefined;
    message.currentPhaseId = object.currentPhaseId ?? "";
    message.capabilities = object.capabilities?.map((e) => CapabilityState.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityState(): CapabilityState {
  return { capability: 0, enabled: false };
}

export const CapabilityState: MessageFns<CapabilityState> = {
  encode(message: CapabilityState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capability !== 0) {
      writer.uint32(8).int32(message.capability);
    }
    if (message.enabled !== false) {
      writer.uint32(16).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.capability = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityState {
    return {
      capability: isSet(object.capability) ? capabilityFromJSON(object.capability) : 0,
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
    };
  },

  toJSON(message: CapabilityState): unknown {
    const obj: any = {};
    if (message.capability !== 0) {
      obj.capability = capabilityToJSON(message.capability);
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<CapabilityState>): CapabilityState {
    return CapabilityState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CapabilityState>): CapabilityState {
    const message = createBaseCapabilityState();
    message.capability = object.capability ?? 0;
    message.enabled = object.enabled ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
