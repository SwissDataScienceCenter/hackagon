// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: hackathon/entities/hackathon_member.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { User } from "../../user/entities/user";
import { HackathonRole, hackathonRoleFromJSON, hackathonRoleToJSON } from "./hackathon_role";

export const protobufPackage = "hackathon.entities";

/**
 * A user's relationship to a hackathon: DB participation state (is_waiting, joined_at)
 * combined with casbin role. `role` is HACKATHON_ROLE_UNSPECIFIED for users with no
 * casbin role for this hackathon; `is_waiting` is false once approved.
 */
export interface HackathonMember {
  user: User | undefined;
  role: HackathonRole;
  isWaiting: boolean;
  joinedAt: Date | undefined;
}

function createBaseHackathonMember(): HackathonMember {
  return { user: undefined, role: 0, isWaiting: false, joinedAt: undefined };
}

export const HackathonMember: MessageFns<HackathonMember> = {
  encode(message: HackathonMember, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(10).fork()).join();
    }
    if (message.role !== 0) {
      writer.uint32(16).int32(message.role);
    }
    if (message.isWaiting !== false) {
      writer.uint32(24).bool(message.isWaiting);
    }
    if (message.joinedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.joinedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HackathonMember {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHackathonMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.role = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.isWaiting = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.joinedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HackathonMember {
    return {
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
      role: isSet(object.role) ? hackathonRoleFromJSON(object.role) : 0,
      isWaiting: isSet(object.isWaiting)
        ? globalThis.Boolean(object.isWaiting)
        : isSet(object.is_waiting)
        ? globalThis.Boolean(object.is_waiting)
        : false,
      joinedAt: isSet(object.joinedAt)
        ? fromJsonTimestamp(object.joinedAt)
        : isSet(object.joined_at)
        ? fromJsonTimestamp(object.joined_at)
        : undefined,
    };
  },

  toJSON(message: HackathonMember): unknown {
    const obj: any = {};
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    if (message.role !== 0) {
      obj.role = hackathonRoleToJSON(message.role);
    }
    if (message.isWaiting !== false) {
      obj.isWaiting = message.isWaiting;
    }
    if (message.joinedAt !== undefined) {
      obj.joinedAt = message.joinedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<HackathonMember>): HackathonMember {
    return HackathonMember.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HackathonMember>): HackathonMember {
    const message = createBaseHackathonMember();
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    message.role = object.role ?? 0;
    message.isWaiting = object.isWaiting ?? false;
    message.joinedAt = object.joinedAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
