// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/submission"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/votecategory"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/voteresult"
)

// A placement entry within a vote category. Multiple VoteResults can exist per category.
type VoteResult struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Ordering hint (1 = first place, 2 = second, etc.). Not unique — ties allowed.
	Position int `json:"position,omitempty"`
	// Optional custom title for the placement (e.g. "Most Innovative").
	Title string `json:"title,omitempty"`
	// Timestamp when the result was created.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of the last modification.
	ModifiedAt time.Time `json:"modified_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VoteResultQuery when eager-loading is set.
	Edges                   VoteResultEdges `json:"edges"`
	submission_vote_results *uuid.UUID
	vote_category_results   *uuid.UUID
	selectValues            sql.SelectValues
}

// VoteResultEdges holds the relations/edges for other nodes in the graph.
type VoteResultEdges struct {
	// The category this result belongs to.
	VoteCategory *VoteCategory `json:"vote_category,omitempty"`
	// The submission being placed.
	Submission *Submission `json:"submission,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// VoteCategoryOrErr returns the VoteCategory value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteResultEdges) VoteCategoryOrErr() (*VoteCategory, error) {
	if e.VoteCategory != nil {
		return e.VoteCategory, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: votecategory.Label}
	}
	return nil, &NotLoadedError{edge: "vote_category"}
}

// SubmissionOrErr returns the Submission value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteResultEdges) SubmissionOrErr() (*Submission, error) {
	if e.Submission != nil {
		return e.Submission, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: submission.Label}
	}
	return nil, &NotLoadedError{edge: "submission"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VoteResult) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case voteresult.FieldPosition:
			values[i] = new(sql.NullInt64)
		case voteresult.FieldTitle:
			values[i] = new(sql.NullString)
		case voteresult.FieldCreatedAt, voteresult.FieldModifiedAt:
			values[i] = new(sql.NullTime)
		case voteresult.FieldID:
			values[i] = new(uuid.UUID)
		case voteresult.ForeignKeys[0]: // submission_vote_results
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case voteresult.ForeignKeys[1]: // vote_category_results
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VoteResult fields.
func (_m *VoteResult) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case voteresult.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case voteresult.FieldPosition:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field position", values[i])
			} else if value.Valid {
				_m.Position = int(value.Int64)
			}
		case voteresult.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case voteresult.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case voteresult.FieldModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified_at", values[i])
			} else if value.Valid {
				_m.ModifiedAt = value.Time
			}
		case voteresult.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field submission_vote_results", values[i])
			} else if value.Valid {
				_m.submission_vote_results = new(uuid.UUID)
				*_m.submission_vote_results = *value.S.(*uuid.UUID)
			}
		case voteresult.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field vote_category_results", values[i])
			} else if value.Valid {
				_m.vote_category_results = new(uuid.UUID)
				*_m.vote_category_results = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VoteResult.
// This includes values selected through modifiers, order, etc.
func (_m *VoteResult) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryVoteCategory queries the "vote_category" edge of the VoteResult entity.
func (_m *VoteResult) QueryVoteCategory() *VoteCategoryQuery {
	return NewVoteResultClient(_m.config).QueryVoteCategory(_m)
}

// QuerySubmission queries the "submission" edge of the VoteResult entity.
func (_m *VoteResult) QuerySubmission() *SubmissionQuery {
	return NewVoteResultClient(_m.config).QuerySubmission(_m)
}

// Update returns a builder for updating this VoteResult.
// Note that you need to call VoteResult.Unwrap() before calling this method if this VoteResult
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VoteResult) Update() *VoteResultUpdateOne {
	return NewVoteResultClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VoteResult entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VoteResult) Unwrap() *VoteResult {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VoteResult is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VoteResult) String() string {
	var builder strings.Builder
	builder.WriteString("VoteResult(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("position=")
	builder.WriteString(fmt.Sprintf("%v", _m.Position))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified_at=")
	builder.WriteString(_m.ModifiedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VoteResults is a parsable slice of VoteResult.
type VoteResults []*VoteResult
