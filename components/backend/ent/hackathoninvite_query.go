// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathon"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathoninvite"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/predicate"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/user"
)

// HackathonInviteQuery is the builder for querying HackathonInvite entities.
type HackathonInviteQuery struct {
	config
	ctx           *QueryContext
	order         []hackathoninvite.OrderOption
	inters        []Interceptor
	predicates    []predicate.HackathonInvite
	withHackathon *HackathonQuery
	withCreator   *UserQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HackathonInviteQuery builder.
func (_q *HackathonInviteQuery) Where(ps ...predicate.HackathonInvite) *HackathonInviteQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *HackathonInviteQuery) Limit(limit int) *HackathonInviteQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *HackathonInviteQuery) Offset(offset int) *HackathonInviteQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *HackathonInviteQuery) Unique(unique bool) *HackathonInviteQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *HackathonInviteQuery) Order(o ...hackathoninvite.OrderOption) *HackathonInviteQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryHackathon chains the current query on the "hackathon" edge.
func (_q *HackathonInviteQuery) QueryHackathon() *HackathonQuery {
	query := (&HackathonClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathoninvite.Table, hackathoninvite.FieldID, selector),
			sqlgraph.To(hackathon.Table, hackathon.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, hackathoninvite.HackathonTable, hackathoninvite.HackathonColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreator chains the current query on the "creator" edge.
func (_q *HackathonInviteQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathoninvite.Table, hackathoninvite.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hackathoninvite.CreatorTable, hackathoninvite.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first HackathonInvite entity from the query.
// Returns a *NotFoundError when no HackathonInvite was found.
func (_q *HackathonInviteQuery) First(ctx context.Context) (*HackathonInvite, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hackathoninvite.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *HackathonInviteQuery) FirstX(ctx context.Context) *HackathonInvite {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HackathonInvite ID from the query.
// Returns a *NotFoundError when no HackathonInvite ID was found.
func (_q *HackathonInviteQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hackathoninvite.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *HackathonInviteQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HackathonInvite entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HackathonInvite entity is found.
// Returns a *NotFoundError when no HackathonInvite entities are found.
func (_q *HackathonInviteQuery) Only(ctx context.Context) (*HackathonInvite, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hackathoninvite.Label}
	default:
		return nil, &NotSingularError{hackathoninvite.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *HackathonInviteQuery) OnlyX(ctx context.Context) *HackathonInvite {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HackathonInvite ID in the query.
// Returns a *NotSingularError when more than one HackathonInvite ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *HackathonInviteQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hackathoninvite.Label}
	default:
		err = &NotSingularError{hackathoninvite.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *HackathonInviteQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HackathonInvites.
func (_q *HackathonInviteQuery) All(ctx context.Context) ([]*HackathonInvite, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HackathonInvite, *HackathonInviteQuery]()
	return withInterceptors[[]*HackathonInvite](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *HackathonInviteQuery) AllX(ctx context.Context) []*HackathonInvite {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HackathonInvite IDs.
func (_q *HackathonInviteQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(hackathoninvite.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *HackathonInviteQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *HackathonInviteQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*HackathonInviteQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *HackathonInviteQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *HackathonInviteQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *HackathonInviteQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HackathonInviteQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *HackathonInviteQuery) Clone() *HackathonInviteQuery {
	if _q == nil {
		return nil
	}
	return &HackathonInviteQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]hackathoninvite.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.HackathonInvite{}, _q.predicates...),
		withHackathon: _q.withHackathon.Clone(),
		withCreator:   _q.withCreator.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithHackathon tells the query-builder to eager-load the nodes that are connected to
// the "hackathon" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonInviteQuery) WithHackathon(opts ...func(*HackathonQuery)) *HackathonInviteQuery {
	query := (&HackathonClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHackathon = query
	return _q
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonInviteQuery) WithCreator(opts ...func(*UserQuery)) *HackathonInviteQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreator = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HackathonInvite.Query().
//		GroupBy(hackathoninvite.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *HackathonInviteQuery) GroupBy(field string, fields ...string) *HackathonInviteGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HackathonInviteGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = hackathoninvite.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.HackathonInvite.Query().
//		Select(hackathoninvite.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *HackathonInviteQuery) Select(fields ...string) *HackathonInviteSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &HackathonInviteSelect{HackathonInviteQuery: _q}
	sbuild.label = hackathoninvite.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HackathonInviteSelect configured with the given aggregations.
func (_q *HackathonInviteQuery) Aggregate(fns ...AggregateFunc) *HackathonInviteSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *HackathonInviteQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !hackathoninvite.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *HackathonInviteQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HackathonInvite, error) {
	var (
		nodes       = []*HackathonInvite{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withHackathon != nil,
			_q.withCreator != nil,
		}
	)
	if _q.withHackathon != nil || _q.withCreator != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, hackathoninvite.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HackathonInvite).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HackathonInvite{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withHackathon; query != nil {
		if err := _q.loadHackathon(ctx, query, nodes, nil,
			func(n *HackathonInvite, e *Hackathon) { n.Edges.Hackathon = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCreator; query != nil {
		if err := _q.loadCreator(ctx, query, nodes, nil,
			func(n *HackathonInvite, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *HackathonInviteQuery) loadHackathon(ctx context.Context, query *HackathonQuery, nodes []*HackathonInvite, init func(*HackathonInvite), assign func(*HackathonInvite, *Hackathon)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HackathonInvite)
	for i := range nodes {
		if nodes[i].hackathon_invite_hackathon == nil {
			continue
		}
		fk := *nodes[i].hackathon_invite_hackathon
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(hackathon.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "hackathon_invite_hackathon" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *HackathonInviteQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*HackathonInvite, init func(*HackathonInvite), assign func(*HackathonInvite, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HackathonInvite)
	for i := range nodes {
		if nodes[i].user_created_hackathon_invites == nil {
			continue
		}
		fk := *nodes[i].user_created_hackathon_invites
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_created_hackathon_invites" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *HackathonInviteQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *HackathonInviteQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hackathoninvite.Table, hackathoninvite.Columns, sqlgraph.NewFieldSpec(hackathoninvite.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hackathoninvite.FieldID)
		for i := range fields {
			if fields[i] != hackathoninvite.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *HackathonInviteQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(hackathoninvite.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = hackathoninvite.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HackathonInviteGroupBy is the group-by builder for HackathonInvite entities.
type HackathonInviteGroupBy struct {
	selector
	build *HackathonInviteQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *HackathonInviteGroupBy) Aggregate(fns ...AggregateFunc) *HackathonInviteGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *HackathonInviteGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonInviteQuery, *HackathonInviteGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *HackathonInviteGroupBy) sqlScan(ctx context.Context, root *HackathonInviteQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HackathonInviteSelect is the builder for selecting fields of HackathonInvite entities.
type HackathonInviteSelect struct {
	*HackathonInviteQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *HackathonInviteSelect) Aggregate(fns ...AggregateFunc) *HackathonInviteSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *HackathonInviteSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonInviteQuery, *HackathonInviteSelect](ctx, _s.HackathonInviteQuery, _s, _s.inters, v)
}

func (_s *HackathonInviteSelect) sqlScan(ctx context.Context, root *HackathonInviteQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
