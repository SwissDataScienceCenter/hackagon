// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathon"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathoninvite"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/user"
)

// An invite for a hackathon.
type HackathonInvite struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// RevokedAt holds the value of the "revoked_at" field.
	RevokedAt *time.Time `json:"revoked_at,omitempty"`
	// Token holds the value of the "token" field.
	Token uuid.UUID `json:"token,omitempty"`
	// Note holds the value of the "note" field.
	Note string `json:"note,omitempty"`
	// ExpiresAt holds the value of the "expires_at" field.
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HackathonInviteQuery when eager-loading is set.
	Edges                          HackathonInviteEdges `json:"edges"`
	hackathon_invite_hackathon     *uuid.UUID
	user_created_hackathon_invites *uuid.UUID
	selectValues                   sql.SelectValues
}

// HackathonInviteEdges holds the relations/edges for other nodes in the graph.
type HackathonInviteEdges struct {
	// The hackathon this invite grants access to.
	Hackathon *Hackathon `json:"hackathon,omitempty"`
	// The user who created this invite.
	Creator *User `json:"creator,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// HackathonOrErr returns the Hackathon value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HackathonInviteEdges) HackathonOrErr() (*Hackathon, error) {
	if e.Hackathon != nil {
		return e.Hackathon, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: hackathon.Label}
	}
	return nil, &NotLoadedError{edge: "hackathon"}
}

// CreatorOrErr returns the Creator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HackathonInviteEdges) CreatorOrErr() (*User, error) {
	if e.Creator != nil {
		return e.Creator, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "creator"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HackathonInvite) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hackathoninvite.FieldNote:
			values[i] = new(sql.NullString)
		case hackathoninvite.FieldCreatedAt, hackathoninvite.FieldRevokedAt, hackathoninvite.FieldExpiresAt:
			values[i] = new(sql.NullTime)
		case hackathoninvite.FieldID, hackathoninvite.FieldToken:
			values[i] = new(uuid.UUID)
		case hackathoninvite.ForeignKeys[0]: // hackathon_invite_hackathon
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case hackathoninvite.ForeignKeys[1]: // user_created_hackathon_invites
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HackathonInvite fields.
func (_m *HackathonInvite) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hackathoninvite.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case hackathoninvite.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case hackathoninvite.FieldRevokedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field revoked_at", values[i])
			} else if value.Valid {
				_m.RevokedAt = new(time.Time)
				*_m.RevokedAt = value.Time
			}
		case hackathoninvite.FieldToken:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value != nil {
				_m.Token = *value
			}
		case hackathoninvite.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				_m.Note = value.String
			}
		case hackathoninvite.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = new(time.Time)
				*_m.ExpiresAt = value.Time
			}
		case hackathoninvite.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field hackathon_invite_hackathon", values[i])
			} else if value.Valid {
				_m.hackathon_invite_hackathon = new(uuid.UUID)
				*_m.hackathon_invite_hackathon = *value.S.(*uuid.UUID)
			}
		case hackathoninvite.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_created_hackathon_invites", values[i])
			} else if value.Valid {
				_m.user_created_hackathon_invites = new(uuid.UUID)
				*_m.user_created_hackathon_invites = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HackathonInvite.
// This includes values selected through modifiers, order, etc.
func (_m *HackathonInvite) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryHackathon queries the "hackathon" edge of the HackathonInvite entity.
func (_m *HackathonInvite) QueryHackathon() *HackathonQuery {
	return NewHackathonInviteClient(_m.config).QueryHackathon(_m)
}

// QueryCreator queries the "creator" edge of the HackathonInvite entity.
func (_m *HackathonInvite) QueryCreator() *UserQuery {
	return NewHackathonInviteClient(_m.config).QueryCreator(_m)
}

// Update returns a builder for updating this HackathonInvite.
// Note that you need to call HackathonInvite.Unwrap() before calling this method if this HackathonInvite
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HackathonInvite) Update() *HackathonInviteUpdateOne {
	return NewHackathonInviteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HackathonInvite entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HackathonInvite) Unwrap() *HackathonInvite {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: HackathonInvite is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HackathonInvite) String() string {
	var builder strings.Builder
	builder.WriteString("HackathonInvite(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RevokedAt; v != nil {
		builder.WriteString("revoked_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("token=")
	builder.WriteString(fmt.Sprintf("%v", _m.Token))
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(_m.Note)
	builder.WriteString(", ")
	if v := _m.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// HackathonInvites is a parsable slice of HackathonInvite.
type HackathonInvites []*HackathonInvite
