// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathon"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathonstate"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/phase"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/predicate"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/user"
)

// HackathonStateQuery is the builder for querying HackathonState entities.
type HackathonStateQuery struct {
	config
	ctx              *QueryContext
	order            []hackathonstate.OrderOption
	inters           []Interceptor
	predicates       []predicate.HackathonState
	withHackathon    *HackathonQuery
	withModifier     *UserQuery
	withCurrentPhase *PhaseQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HackathonStateQuery builder.
func (_q *HackathonStateQuery) Where(ps ...predicate.HackathonState) *HackathonStateQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *HackathonStateQuery) Limit(limit int) *HackathonStateQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *HackathonStateQuery) Offset(offset int) *HackathonStateQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *HackathonStateQuery) Unique(unique bool) *HackathonStateQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *HackathonStateQuery) Order(o ...hackathonstate.OrderOption) *HackathonStateQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryHackathon chains the current query on the "hackathon" edge.
func (_q *HackathonStateQuery) QueryHackathon() *HackathonQuery {
	query := (&HackathonClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathonstate.Table, hackathonstate.FieldID, selector),
			sqlgraph.To(hackathon.Table, hackathon.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, hackathonstate.HackathonTable, hackathonstate.HackathonColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModifier chains the current query on the "modifier" edge.
func (_q *HackathonStateQuery) QueryModifier() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathonstate.Table, hackathonstate.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hackathonstate.ModifierTable, hackathonstate.ModifierColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCurrentPhase chains the current query on the "current_phase" edge.
func (_q *HackathonStateQuery) QueryCurrentPhase() *PhaseQuery {
	query := (&PhaseClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathonstate.Table, hackathonstate.FieldID, selector),
			sqlgraph.To(phase.Table, phase.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, hackathonstate.CurrentPhaseTable, hackathonstate.CurrentPhaseColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first HackathonState entity from the query.
// Returns a *NotFoundError when no HackathonState was found.
func (_q *HackathonStateQuery) First(ctx context.Context) (*HackathonState, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hackathonstate.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *HackathonStateQuery) FirstX(ctx context.Context) *HackathonState {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first HackathonState ID from the query.
// Returns a *NotFoundError when no HackathonState ID was found.
func (_q *HackathonStateQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hackathonstate.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *HackathonStateQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single HackathonState entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one HackathonState entity is found.
// Returns a *NotFoundError when no HackathonState entities are found.
func (_q *HackathonStateQuery) Only(ctx context.Context) (*HackathonState, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hackathonstate.Label}
	default:
		return nil, &NotSingularError{hackathonstate.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *HackathonStateQuery) OnlyX(ctx context.Context) *HackathonState {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only HackathonState ID in the query.
// Returns a *NotSingularError when more than one HackathonState ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *HackathonStateQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hackathonstate.Label}
	default:
		err = &NotSingularError{hackathonstate.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *HackathonStateQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of HackathonStates.
func (_q *HackathonStateQuery) All(ctx context.Context) ([]*HackathonState, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*HackathonState, *HackathonStateQuery]()
	return withInterceptors[[]*HackathonState](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *HackathonStateQuery) AllX(ctx context.Context) []*HackathonState {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of HackathonState IDs.
func (_q *HackathonStateQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(hackathonstate.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *HackathonStateQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *HackathonStateQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*HackathonStateQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *HackathonStateQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *HackathonStateQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *HackathonStateQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HackathonStateQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *HackathonStateQuery) Clone() *HackathonStateQuery {
	if _q == nil {
		return nil
	}
	return &HackathonStateQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]hackathonstate.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.HackathonState{}, _q.predicates...),
		withHackathon:    _q.withHackathon.Clone(),
		withModifier:     _q.withModifier.Clone(),
		withCurrentPhase: _q.withCurrentPhase.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithHackathon tells the query-builder to eager-load the nodes that are connected to
// the "hackathon" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonStateQuery) WithHackathon(opts ...func(*HackathonQuery)) *HackathonStateQuery {
	query := (&HackathonClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withHackathon = query
	return _q
}

// WithModifier tells the query-builder to eager-load the nodes that are connected to
// the "modifier" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonStateQuery) WithModifier(opts ...func(*UserQuery)) *HackathonStateQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withModifier = query
	return _q
}

// WithCurrentPhase tells the query-builder to eager-load the nodes that are connected to
// the "current_phase" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonStateQuery) WithCurrentPhase(opts ...func(*PhaseQuery)) *HackathonStateQuery {
	query := (&PhaseClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCurrentPhase = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RegistrationsEnabled bool `json:"registrations_enabled,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.HackathonState.Query().
//		GroupBy(hackathonstate.FieldRegistrationsEnabled).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *HackathonStateQuery) GroupBy(field string, fields ...string) *HackathonStateGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HackathonStateGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = hackathonstate.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RegistrationsEnabled bool `json:"registrations_enabled,omitempty"`
//	}
//
//	client.HackathonState.Query().
//		Select(hackathonstate.FieldRegistrationsEnabled).
//		Scan(ctx, &v)
func (_q *HackathonStateQuery) Select(fields ...string) *HackathonStateSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &HackathonStateSelect{HackathonStateQuery: _q}
	sbuild.label = hackathonstate.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HackathonStateSelect configured with the given aggregations.
func (_q *HackathonStateQuery) Aggregate(fns ...AggregateFunc) *HackathonStateSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *HackathonStateQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !hackathonstate.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *HackathonStateQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*HackathonState, error) {
	var (
		nodes       = []*HackathonState{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withHackathon != nil,
			_q.withModifier != nil,
			_q.withCurrentPhase != nil,
		}
	)
	if _q.withHackathon != nil || _q.withModifier != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, hackathonstate.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*HackathonState).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &HackathonState{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withHackathon; query != nil {
		if err := _q.loadHackathon(ctx, query, nodes, nil,
			func(n *HackathonState, e *Hackathon) { n.Edges.Hackathon = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withModifier; query != nil {
		if err := _q.loadModifier(ctx, query, nodes, nil,
			func(n *HackathonState, e *User) { n.Edges.Modifier = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCurrentPhase; query != nil {
		if err := _q.loadCurrentPhase(ctx, query, nodes, nil,
			func(n *HackathonState, e *Phase) { n.Edges.CurrentPhase = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *HackathonStateQuery) loadHackathon(ctx context.Context, query *HackathonQuery, nodes []*HackathonState, init func(*HackathonState), assign func(*HackathonState, *Hackathon)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HackathonState)
	for i := range nodes {
		if nodes[i].hackathon_state == nil {
			continue
		}
		fk := *nodes[i].hackathon_state
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(hackathon.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "hackathon_state" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *HackathonStateQuery) loadModifier(ctx context.Context, query *UserQuery, nodes []*HackathonState, init func(*HackathonState), assign func(*HackathonState, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HackathonState)
	for i := range nodes {
		if nodes[i].user_modified_states == nil {
			continue
		}
		fk := *nodes[i].user_modified_states
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_modified_states" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *HackathonStateQuery) loadCurrentPhase(ctx context.Context, query *PhaseQuery, nodes []*HackathonState, init func(*HackathonState), assign func(*HackathonState, *Phase)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*HackathonState)
	for i := range nodes {
		fk := nodes[i].CurrentPhaseID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(phase.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "current_phase_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *HackathonStateQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *HackathonStateQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hackathonstate.Table, hackathonstate.Columns, sqlgraph.NewFieldSpec(hackathonstate.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hackathonstate.FieldID)
		for i := range fields {
			if fields[i] != hackathonstate.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCurrentPhase != nil {
			_spec.Node.AddColumnOnce(hackathonstate.FieldCurrentPhaseID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *HackathonStateQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(hackathonstate.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = hackathonstate.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HackathonStateGroupBy is the group-by builder for HackathonState entities.
type HackathonStateGroupBy struct {
	selector
	build *HackathonStateQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *HackathonStateGroupBy) Aggregate(fns ...AggregateFunc) *HackathonStateGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *HackathonStateGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonStateQuery, *HackathonStateGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *HackathonStateGroupBy) sqlScan(ctx context.Context, root *HackathonStateQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HackathonStateSelect is the builder for selecting fields of HackathonState entities.
type HackathonStateSelect struct {
	*HackathonStateQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *HackathonStateSelect) Aggregate(fns ...AggregateFunc) *HackathonStateSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *HackathonStateSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonStateQuery, *HackathonStateSelect](ctx, _s.HackathonStateQuery, _s, _s.inters, v)
}

func (_s *HackathonStateSelect) sqlScan(ctx context.Context, root *HackathonStateQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
