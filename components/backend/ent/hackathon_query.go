// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathon"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathonstate"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/page"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/participant"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/phase"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/predicate"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/project"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/question"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/track"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/user"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/votecategory"
)

// HackathonQuery is the builder for querying Hackathon entities.
type HackathonQuery struct {
	config
	ctx                    *QueryContext
	order                  []hackathon.OrderOption
	inters                 []Interceptor
	predicates             []predicate.Hackathon
	withTracks             *TrackQuery
	withProjects           *ProjectQuery
	withParticipatingUsers *UserQuery
	withPages              *PageQuery
	withPhases             *PhaseQuery
	withState              *HackathonStateQuery
	withVoteCategories     *VoteCategoryQuery
	withQuestions          *QuestionQuery
	withOwners             *UserQuery
	withCreator            *UserQuery
	withModifier           *UserQuery
	withParticipants       *ParticipantQuery
	withFKs                bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HackathonQuery builder.
func (_q *HackathonQuery) Where(ps ...predicate.Hackathon) *HackathonQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *HackathonQuery) Limit(limit int) *HackathonQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *HackathonQuery) Offset(offset int) *HackathonQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *HackathonQuery) Unique(unique bool) *HackathonQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *HackathonQuery) Order(o ...hackathon.OrderOption) *HackathonQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryTracks chains the current query on the "tracks" edge.
func (_q *HackathonQuery) QueryTracks() *TrackQuery {
	query := (&TrackClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(track.Table, track.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.TracksTable, hackathon.TracksColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProjects chains the current query on the "projects" edge.
func (_q *HackathonQuery) QueryProjects() *ProjectQuery {
	query := (&ProjectClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.ProjectsTable, hackathon.ProjectsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParticipatingUsers chains the current query on the "participating_users" edge.
func (_q *HackathonQuery) QueryParticipatingUsers() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, hackathon.ParticipatingUsersTable, hackathon.ParticipatingUsersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPages chains the current query on the "pages" edge.
func (_q *HackathonQuery) QueryPages() *PageQuery {
	query := (&PageClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(page.Table, page.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.PagesTable, hackathon.PagesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPhases chains the current query on the "phases" edge.
func (_q *HackathonQuery) QueryPhases() *PhaseQuery {
	query := (&PhaseClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(phase.Table, phase.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.PhasesTable, hackathon.PhasesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryState chains the current query on the "state" edge.
func (_q *HackathonQuery) QueryState() *HackathonStateQuery {
	query := (&HackathonStateClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(hackathonstate.Table, hackathonstate.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, hackathon.StateTable, hackathon.StateColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryVoteCategories chains the current query on the "vote_categories" edge.
func (_q *HackathonQuery) QueryVoteCategories() *VoteCategoryQuery {
	query := (&VoteCategoryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(votecategory.Table, votecategory.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.VoteCategoriesTable, hackathon.VoteCategoriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQuestions chains the current query on the "questions" edge.
func (_q *HackathonQuery) QueryQuestions() *QuestionQuery {
	query := (&QuestionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(question.Table, question.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, hackathon.QuestionsTable, hackathon.QuestionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOwners chains the current query on the "owners" edge.
func (_q *HackathonQuery) QueryOwners() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, hackathon.OwnersTable, hackathon.OwnersPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreator chains the current query on the "creator" edge.
func (_q *HackathonQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hackathon.CreatorTable, hackathon.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryModifier chains the current query on the "modifier" edge.
func (_q *HackathonQuery) QueryModifier() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hackathon.ModifierTable, hackathon.ModifierColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryParticipants chains the current query on the "participants" edge.
func (_q *HackathonQuery) QueryParticipants() *ParticipantQuery {
	query := (&ParticipantClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hackathon.Table, hackathon.FieldID, selector),
			sqlgraph.To(participant.Table, participant.HackathonColumn),
			sqlgraph.Edge(sqlgraph.O2M, true, hackathon.ParticipantsTable, hackathon.ParticipantsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Hackathon entity from the query.
// Returns a *NotFoundError when no Hackathon was found.
func (_q *HackathonQuery) First(ctx context.Context) (*Hackathon, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hackathon.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *HackathonQuery) FirstX(ctx context.Context) *Hackathon {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Hackathon ID from the query.
// Returns a *NotFoundError when no Hackathon ID was found.
func (_q *HackathonQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hackathon.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *HackathonQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Hackathon entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Hackathon entity is found.
// Returns a *NotFoundError when no Hackathon entities are found.
func (_q *HackathonQuery) Only(ctx context.Context) (*Hackathon, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hackathon.Label}
	default:
		return nil, &NotSingularError{hackathon.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *HackathonQuery) OnlyX(ctx context.Context) *Hackathon {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Hackathon ID in the query.
// Returns a *NotSingularError when more than one Hackathon ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *HackathonQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hackathon.Label}
	default:
		err = &NotSingularError{hackathon.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *HackathonQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Hackathons.
func (_q *HackathonQuery) All(ctx context.Context) ([]*Hackathon, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Hackathon, *HackathonQuery]()
	return withInterceptors[[]*Hackathon](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *HackathonQuery) AllX(ctx context.Context) []*Hackathon {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Hackathon IDs.
func (_q *HackathonQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(hackathon.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *HackathonQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *HackathonQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*HackathonQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *HackathonQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *HackathonQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *HackathonQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HackathonQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *HackathonQuery) Clone() *HackathonQuery {
	if _q == nil {
		return nil
	}
	return &HackathonQuery{
		config:                 _q.config,
		ctx:                    _q.ctx.Clone(),
		order:                  append([]hackathon.OrderOption{}, _q.order...),
		inters:                 append([]Interceptor{}, _q.inters...),
		predicates:             append([]predicate.Hackathon{}, _q.predicates...),
		withTracks:             _q.withTracks.Clone(),
		withProjects:           _q.withProjects.Clone(),
		withParticipatingUsers: _q.withParticipatingUsers.Clone(),
		withPages:              _q.withPages.Clone(),
		withPhases:             _q.withPhases.Clone(),
		withState:              _q.withState.Clone(),
		withVoteCategories:     _q.withVoteCategories.Clone(),
		withQuestions:          _q.withQuestions.Clone(),
		withOwners:             _q.withOwners.Clone(),
		withCreator:            _q.withCreator.Clone(),
		withModifier:           _q.withModifier.Clone(),
		withParticipants:       _q.withParticipants.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithTracks tells the query-builder to eager-load the nodes that are connected to
// the "tracks" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithTracks(opts ...func(*TrackQuery)) *HackathonQuery {
	query := (&TrackClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withTracks = query
	return _q
}

// WithProjects tells the query-builder to eager-load the nodes that are connected to
// the "projects" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithProjects(opts ...func(*ProjectQuery)) *HackathonQuery {
	query := (&ProjectClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withProjects = query
	return _q
}

// WithParticipatingUsers tells the query-builder to eager-load the nodes that are connected to
// the "participating_users" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithParticipatingUsers(opts ...func(*UserQuery)) *HackathonQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParticipatingUsers = query
	return _q
}

// WithPages tells the query-builder to eager-load the nodes that are connected to
// the "pages" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithPages(opts ...func(*PageQuery)) *HackathonQuery {
	query := (&PageClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPages = query
	return _q
}

// WithPhases tells the query-builder to eager-load the nodes that are connected to
// the "phases" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithPhases(opts ...func(*PhaseQuery)) *HackathonQuery {
	query := (&PhaseClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPhases = query
	return _q
}

// WithState tells the query-builder to eager-load the nodes that are connected to
// the "state" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithState(opts ...func(*HackathonStateQuery)) *HackathonQuery {
	query := (&HackathonStateClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withState = query
	return _q
}

// WithVoteCategories tells the query-builder to eager-load the nodes that are connected to
// the "vote_categories" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithVoteCategories(opts ...func(*VoteCategoryQuery)) *HackathonQuery {
	query := (&VoteCategoryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVoteCategories = query
	return _q
}

// WithQuestions tells the query-builder to eager-load the nodes that are connected to
// the "questions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithQuestions(opts ...func(*QuestionQuery)) *HackathonQuery {
	query := (&QuestionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQuestions = query
	return _q
}

// WithOwners tells the query-builder to eager-load the nodes that are connected to
// the "owners" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithOwners(opts ...func(*UserQuery)) *HackathonQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOwners = query
	return _q
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithCreator(opts ...func(*UserQuery)) *HackathonQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCreator = query
	return _q
}

// WithModifier tells the query-builder to eager-load the nodes that are connected to
// the "modifier" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithModifier(opts ...func(*UserQuery)) *HackathonQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withModifier = query
	return _q
}

// WithParticipants tells the query-builder to eager-load the nodes that are connected to
// the "participants" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *HackathonQuery) WithParticipants(opts ...func(*ParticipantQuery)) *HackathonQuery {
	query := (&ParticipantClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withParticipants = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Hackathon.Query().
//		GroupBy(hackathon.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *HackathonQuery) GroupBy(field string, fields ...string) *HackathonGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HackathonGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = hackathon.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Hackathon.Query().
//		Select(hackathon.FieldName).
//		Scan(ctx, &v)
func (_q *HackathonQuery) Select(fields ...string) *HackathonSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &HackathonSelect{HackathonQuery: _q}
	sbuild.label = hackathon.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HackathonSelect configured with the given aggregations.
func (_q *HackathonQuery) Aggregate(fns ...AggregateFunc) *HackathonSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *HackathonQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !hackathon.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *HackathonQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Hackathon, error) {
	var (
		nodes       = []*Hackathon{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [12]bool{
			_q.withTracks != nil,
			_q.withProjects != nil,
			_q.withParticipatingUsers != nil,
			_q.withPages != nil,
			_q.withPhases != nil,
			_q.withState != nil,
			_q.withVoteCategories != nil,
			_q.withQuestions != nil,
			_q.withOwners != nil,
			_q.withCreator != nil,
			_q.withModifier != nil,
			_q.withParticipants != nil,
		}
	)
	if _q.withCreator != nil || _q.withModifier != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, hackathon.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Hackathon).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Hackathon{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withTracks; query != nil {
		if err := _q.loadTracks(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Tracks = []*Track{} },
			func(n *Hackathon, e *Track) { n.Edges.Tracks = append(n.Edges.Tracks, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withProjects; query != nil {
		if err := _q.loadProjects(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Projects = []*Project{} },
			func(n *Hackathon, e *Project) { n.Edges.Projects = append(n.Edges.Projects, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParticipatingUsers; query != nil {
		if err := _q.loadParticipatingUsers(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.ParticipatingUsers = []*User{} },
			func(n *Hackathon, e *User) { n.Edges.ParticipatingUsers = append(n.Edges.ParticipatingUsers, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPages; query != nil {
		if err := _q.loadPages(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Pages = []*Page{} },
			func(n *Hackathon, e *Page) { n.Edges.Pages = append(n.Edges.Pages, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withPhases; query != nil {
		if err := _q.loadPhases(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Phases = []*Phase{} },
			func(n *Hackathon, e *Phase) { n.Edges.Phases = append(n.Edges.Phases, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withState; query != nil {
		if err := _q.loadState(ctx, query, nodes, nil,
			func(n *Hackathon, e *HackathonState) { n.Edges.State = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withVoteCategories; query != nil {
		if err := _q.loadVoteCategories(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.VoteCategories = []*VoteCategory{} },
			func(n *Hackathon, e *VoteCategory) { n.Edges.VoteCategories = append(n.Edges.VoteCategories, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withQuestions; query != nil {
		if err := _q.loadQuestions(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Questions = []*Question{} },
			func(n *Hackathon, e *Question) { n.Edges.Questions = append(n.Edges.Questions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOwners; query != nil {
		if err := _q.loadOwners(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Owners = []*User{} },
			func(n *Hackathon, e *User) { n.Edges.Owners = append(n.Edges.Owners, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCreator; query != nil {
		if err := _q.loadCreator(ctx, query, nodes, nil,
			func(n *Hackathon, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withModifier; query != nil {
		if err := _q.loadModifier(ctx, query, nodes, nil,
			func(n *Hackathon, e *User) { n.Edges.Modifier = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withParticipants; query != nil {
		if err := _q.loadParticipants(ctx, query, nodes,
			func(n *Hackathon) { n.Edges.Participants = []*Participant{} },
			func(n *Hackathon, e *Participant) { n.Edges.Participants = append(n.Edges.Participants, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *HackathonQuery) loadTracks(ctx context.Context, query *TrackQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Track)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Track(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.TracksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_tracks
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_tracks" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_tracks" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadProjects(ctx context.Context, query *ProjectQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Project)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Project(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.ProjectsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_projects
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_projects" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_projects" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadParticipatingUsers(ctx context.Context, query *UserQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *User)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*Hackathon)
	nids := make(map[uuid.UUID]map[*Hackathon]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(hackathon.ParticipatingUsersTable)
		s.Join(joinT).On(s.C(user.FieldID), joinT.C(hackathon.ParticipatingUsersPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(hackathon.ParticipatingUsersPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(hackathon.ParticipatingUsersPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*Hackathon]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*User](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "participating_users" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *HackathonQuery) loadPages(ctx context.Context, query *PageQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Page)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Page(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.PagesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_pages
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_pages" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_pages" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadPhases(ctx context.Context, query *PhaseQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Phase)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Phase(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.PhasesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_phases
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_phases" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_phases" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadState(ctx context.Context, query *HackathonStateQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *HackathonState)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.HackathonState(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.StateColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_state
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_state" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_state" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadVoteCategories(ctx context.Context, query *VoteCategoryQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *VoteCategory)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.VoteCategory(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.VoteCategoriesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.hackathon_vote_categories
		if fk == nil {
			return fmt.Errorf(`foreign-key "hackathon_vote_categories" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_vote_categories" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadQuestions(ctx context.Context, query *QuestionQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Question)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(question.FieldHackathonID)
	}
	query.Where(predicate.Question(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.QuestionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HackathonID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *HackathonQuery) loadOwners(ctx context.Context, query *UserQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *User)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*Hackathon)
	nids := make(map[uuid.UUID]map[*Hackathon]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(hackathon.OwnersTable)
		s.Join(joinT).On(s.C(user.FieldID), joinT.C(hackathon.OwnersPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(hackathon.OwnersPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(hackathon.OwnersPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*Hackathon]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*User](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "owners" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *HackathonQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Hackathon)
	for i := range nodes {
		if nodes[i].user_created_hackathons == nil {
			continue
		}
		fk := *nodes[i].user_created_hackathons
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_created_hackathons" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *HackathonQuery) loadModifier(ctx context.Context, query *UserQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Hackathon)
	for i := range nodes {
		if nodes[i].user_modified_hackathons == nil {
			continue
		}
		fk := *nodes[i].user_modified_hackathons
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_modified_hackathons" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *HackathonQuery) loadParticipants(ctx context.Context, query *ParticipantQuery, nodes []*Hackathon, init func(*Hackathon), assign func(*Hackathon, *Participant)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Hackathon)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(participant.FieldHackathonID)
	}
	query.Where(predicate.Participant(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hackathon.ParticipantsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.HackathonID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "hackathon_id" returned %v for node %v`, fk, n)
		}
		assign(node, n)
	}
	return nil
}

func (_q *HackathonQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *HackathonQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hackathon.Table, hackathon.Columns, sqlgraph.NewFieldSpec(hackathon.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hackathon.FieldID)
		for i := range fields {
			if fields[i] != hackathon.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *HackathonQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(hackathon.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = hackathon.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HackathonGroupBy is the group-by builder for Hackathon entities.
type HackathonGroupBy struct {
	selector
	build *HackathonQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *HackathonGroupBy) Aggregate(fns ...AggregateFunc) *HackathonGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *HackathonGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonQuery, *HackathonGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *HackathonGroupBy) sqlScan(ctx context.Context, root *HackathonQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HackathonSelect is the builder for selecting fields of Hackathon entities.
type HackathonSelect struct {
	*HackathonQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *HackathonSelect) Aggregate(fns ...AggregateFunc) *HackathonSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *HackathonSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HackathonQuery, *HackathonSelect](ctx, _s.HackathonQuery, _s, _s.inters, v)
}

func (_s *HackathonSelect) sqlScan(ctx context.Context, root *HackathonQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
