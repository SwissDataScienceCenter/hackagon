// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/submission"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/user"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/vote"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/votecategory"
)

// A single atomic judgment from one voter on one submission within one category.
type Vote struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Discriminator for the vote method.
	VoteType vote.VoteType `json:"vote_type,omitempty"`
	// Rank position (ranked) or points awarded (points-based). Optional for single_choice.
	Value int `json:"value,omitempty"`
	// Timestamp when the vote was created.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of the last modification.
	ModifiedAt time.Time `json:"modified_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VoteQuery when eager-loading is set.
	Edges               VoteEdges `json:"edges"`
	submission_votes    *uuid.UUID
	user_votes          *uuid.UUID
	vote_category_votes *uuid.UUID
	selectValues        sql.SelectValues
}

// VoteEdges holds the relations/edges for other nodes in the graph.
type VoteEdges struct {
	// The vote category this vote belongs to.
	Category *VoteCategory `json:"category,omitempty"`
	// Keycloak user ID of the voter.
	Voter *User `json:"voter,omitempty"`
	// The submission this vote is for.
	Submission *Submission `json:"submission,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// CategoryOrErr returns the Category value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteEdges) CategoryOrErr() (*VoteCategory, error) {
	if e.Category != nil {
		return e.Category, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: votecategory.Label}
	}
	return nil, &NotLoadedError{edge: "category"}
}

// VoterOrErr returns the Voter value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteEdges) VoterOrErr() (*User, error) {
	if e.Voter != nil {
		return e.Voter, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "voter"}
}

// SubmissionOrErr returns the Submission value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteEdges) SubmissionOrErr() (*Submission, error) {
	if e.Submission != nil {
		return e.Submission, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: submission.Label}
	}
	return nil, &NotLoadedError{edge: "submission"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Vote) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case vote.FieldValue:
			values[i] = new(sql.NullInt64)
		case vote.FieldVoteType:
			values[i] = new(sql.NullString)
		case vote.FieldCreatedAt, vote.FieldModifiedAt:
			values[i] = new(sql.NullTime)
		case vote.FieldID:
			values[i] = new(uuid.UUID)
		case vote.ForeignKeys[0]: // submission_votes
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case vote.ForeignKeys[1]: // user_votes
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case vote.ForeignKeys[2]: // vote_category_votes
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Vote fields.
func (_m *Vote) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case vote.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case vote.FieldVoteType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field vote_type", values[i])
			} else if value.Valid {
				_m.VoteType = vote.VoteType(value.String)
			}
		case vote.FieldValue:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				_m.Value = int(value.Int64)
			}
		case vote.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case vote.FieldModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified_at", values[i])
			} else if value.Valid {
				_m.ModifiedAt = value.Time
			}
		case vote.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field submission_votes", values[i])
			} else if value.Valid {
				_m.submission_votes = new(uuid.UUID)
				*_m.submission_votes = *value.S.(*uuid.UUID)
			}
		case vote.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_votes", values[i])
			} else if value.Valid {
				_m.user_votes = new(uuid.UUID)
				*_m.user_votes = *value.S.(*uuid.UUID)
			}
		case vote.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field vote_category_votes", values[i])
			} else if value.Valid {
				_m.vote_category_votes = new(uuid.UUID)
				*_m.vote_category_votes = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the Vote.
// This includes values selected through modifiers, order, etc.
func (_m *Vote) GetValue(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCategory queries the "category" edge of the Vote entity.
func (_m *Vote) QueryCategory() *VoteCategoryQuery {
	return NewVoteClient(_m.config).QueryCategory(_m)
}

// QueryVoter queries the "voter" edge of the Vote entity.
func (_m *Vote) QueryVoter() *UserQuery {
	return NewVoteClient(_m.config).QueryVoter(_m)
}

// QuerySubmission queries the "submission" edge of the Vote entity.
func (_m *Vote) QuerySubmission() *SubmissionQuery {
	return NewVoteClient(_m.config).QuerySubmission(_m)
}

// Update returns a builder for updating this Vote.
// Note that you need to call Vote.Unwrap() before calling this method if this Vote
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Vote) Update() *VoteUpdateOne {
	return NewVoteClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Vote entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Vote) Unwrap() *Vote {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Vote is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Vote) String() string {
	var builder strings.Builder
	builder.WriteString("Vote(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("vote_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.VoteType))
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(fmt.Sprintf("%v", _m.Value))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified_at=")
	builder.WriteString(_m.ModifiedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Votes is a parsable slice of Vote.
type Votes []*Vote
