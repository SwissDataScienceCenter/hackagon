// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/hackathon"
	"github.com/swissdatasciencecenter/hackagon/components/backend/ent/votecategory"
)

// A voting category within a hackathon, defining the criteria and rules for one dimension of evaluation.
type VoteCategory struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Display name of the category (e.g. "Coolness", "Novelty").
	Name string `json:"name,omitempty"`
	// Criteria and instructions for voters.
	Description string `json:"description,omitempty"`
	// How votes are cast: single choice, ranked, or points-based.
	VotingMethod votecategory.VotingMethod `json:"voting_method,omitempty"`
	// Who can vote: all participants or jury only.
	VoterType votecategory.VoterType `json:"voter_type,omitempty"`
	// Maximum points a voter can distribute across submissions (points-based voting only).
	MaxPoints int `json:"max_points,omitempty"`
	// Timestamp when the category was created.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Timestamp of the last modification.
	ModifiedAt time.Time `json:"modified_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the VoteCategoryQuery when eager-loading is set.
	Edges                     VoteCategoryEdges `json:"edges"`
	hackathon_vote_categories *uuid.UUID
	selectValues              sql.SelectValues
}

// VoteCategoryEdges holds the relations/edges for other nodes in the graph.
type VoteCategoryEdges struct {
	// The hackathon this category belongs to.
	Hackathon *Hackathon `json:"hackathon,omitempty"`
	// Users assigned as jury members for this category (M2M). Only used when voter_type is JURY.
	JuryMembers []*User `json:"jury_members,omitempty"`
	// All votes cast for this category.
	Votes []*Vote `json:"votes,omitempty"`
	// Placements assigned to this category.
	Results []*VoteResult `json:"results,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// HackathonOrErr returns the Hackathon value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e VoteCategoryEdges) HackathonOrErr() (*Hackathon, error) {
	if e.Hackathon != nil {
		return e.Hackathon, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: hackathon.Label}
	}
	return nil, &NotLoadedError{edge: "hackathon"}
}

// JuryMembersOrErr returns the JuryMembers value or an error if the edge
// was not loaded in eager-loading.
func (e VoteCategoryEdges) JuryMembersOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.JuryMembers, nil
	}
	return nil, &NotLoadedError{edge: "jury_members"}
}

// VotesOrErr returns the Votes value or an error if the edge
// was not loaded in eager-loading.
func (e VoteCategoryEdges) VotesOrErr() ([]*Vote, error) {
	if e.loadedTypes[2] {
		return e.Votes, nil
	}
	return nil, &NotLoadedError{edge: "votes"}
}

// ResultsOrErr returns the Results value or an error if the edge
// was not loaded in eager-loading.
func (e VoteCategoryEdges) ResultsOrErr() ([]*VoteResult, error) {
	if e.loadedTypes[3] {
		return e.Results, nil
	}
	return nil, &NotLoadedError{edge: "results"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*VoteCategory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case votecategory.FieldMaxPoints:
			values[i] = new(sql.NullInt64)
		case votecategory.FieldName, votecategory.FieldDescription, votecategory.FieldVotingMethod, votecategory.FieldVoterType:
			values[i] = new(sql.NullString)
		case votecategory.FieldCreatedAt, votecategory.FieldModifiedAt:
			values[i] = new(sql.NullTime)
		case votecategory.FieldID:
			values[i] = new(uuid.UUID)
		case votecategory.ForeignKeys[0]: // hackathon_vote_categories
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the VoteCategory fields.
func (_m *VoteCategory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case votecategory.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case votecategory.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case votecategory.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case votecategory.FieldVotingMethod:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field voting_method", values[i])
			} else if value.Valid {
				_m.VotingMethod = votecategory.VotingMethod(value.String)
			}
		case votecategory.FieldVoterType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field voter_type", values[i])
			} else if value.Valid {
				_m.VoterType = votecategory.VoterType(value.String)
			}
		case votecategory.FieldMaxPoints:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field max_points", values[i])
			} else if value.Valid {
				_m.MaxPoints = int(value.Int64)
			}
		case votecategory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case votecategory.FieldModifiedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field modified_at", values[i])
			} else if value.Valid {
				_m.ModifiedAt = value.Time
			}
		case votecategory.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field hackathon_vote_categories", values[i])
			} else if value.Valid {
				_m.hackathon_vote_categories = new(uuid.UUID)
				*_m.hackathon_vote_categories = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the VoteCategory.
// This includes values selected through modifiers, order, etc.
func (_m *VoteCategory) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryHackathon queries the "hackathon" edge of the VoteCategory entity.
func (_m *VoteCategory) QueryHackathon() *HackathonQuery {
	return NewVoteCategoryClient(_m.config).QueryHackathon(_m)
}

// QueryJuryMembers queries the "jury_members" edge of the VoteCategory entity.
func (_m *VoteCategory) QueryJuryMembers() *UserQuery {
	return NewVoteCategoryClient(_m.config).QueryJuryMembers(_m)
}

// QueryVotes queries the "votes" edge of the VoteCategory entity.
func (_m *VoteCategory) QueryVotes() *VoteQuery {
	return NewVoteCategoryClient(_m.config).QueryVotes(_m)
}

// QueryResults queries the "results" edge of the VoteCategory entity.
func (_m *VoteCategory) QueryResults() *VoteResultQuery {
	return NewVoteCategoryClient(_m.config).QueryResults(_m)
}

// Update returns a builder for updating this VoteCategory.
// Note that you need to call VoteCategory.Unwrap() before calling this method if this VoteCategory
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *VoteCategory) Update() *VoteCategoryUpdateOne {
	return NewVoteCategoryClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the VoteCategory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *VoteCategory) Unwrap() *VoteCategory {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: VoteCategory is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *VoteCategory) String() string {
	var builder strings.Builder
	builder.WriteString("VoteCategory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("voting_method=")
	builder.WriteString(fmt.Sprintf("%v", _m.VotingMethod))
	builder.WriteString(", ")
	builder.WriteString("voter_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.VoterType))
	builder.WriteString(", ")
	builder.WriteString("max_points=")
	builder.WriteString(fmt.Sprintf("%v", _m.MaxPoints))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("modified_at=")
	builder.WriteString(_m.ModifiedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// VoteCategories is a parsable slice of VoteCategory.
type VoteCategories []*VoteCategory
