# Generated from tools/configs/keycloak/realm-hackagon.json by harden-realm.py.
# Regenerate it rather than committing a copy that drifts from the dev realm.
realm-hackagon-prod.json
CHANGES.md

# Output of fill-and-encrypt.sh. Contains a live admin password and client
# secret in plain text, and the encrypted bundle built from them. Never commit.
realm-hackagon-prod.filled.json
hackagon-realm-handover.tar
*.age
